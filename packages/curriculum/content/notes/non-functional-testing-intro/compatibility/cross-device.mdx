---
title: "Cross-device"
summary: "Test across meaningful device classes and real hardware constraints, separating viewport emulation from physical-device evidence."
tags: ["non-functional-testing-intro", "compatibility", "track-c"]
updated: "2026-07-18"
related:
  - "non-functional-testing-intro/compatibility/cross-browser"
  - "non-functional-testing-intro/compatibility/responsive-checks"
  - "browser-devtools-mastery/throttling-and-emulation/device-emulation"
---

<Hook>
A narrow desktop window can imitate screen width, but it cannot grow a thumb, a notch, a virtual
keyboard, a weak radio, limited memory, or a high-density touch display. Cross-device testing asks what
changes when software leaves the tester's desk and enters real hands.
</Hook>

<Callout type="analogy">
An emulator is a flight simulator: fast, repeatable, and ideal for many rehearsals. Real hardware is the
aircraft in weather. A responsible test strategy uses the simulator broadly and the aircraft for the
risks the simulator cannot faithfully create.
</Callout>

<Term define="Cross-device testing evaluates important journeys across a risk-based set of device classes and physical characteristics, such as screen and pixel density, touch and keyboard input, orientation, safe areas, memory, sensors, network behavior, and power constraints. Emulation is useful evidence but is not identical to a real device.">Cross-device testing</Term>

## Classify differences before collecting phones

Choose device classes from analytics, audience, support policy, and product features. Cover compact and
large screens, touch and pointer input, relevant densities and orientations, and hardware-dependent
features. Use responsive mode for fast layout sweeps, emulators for controlled OS/device models, device
farms for scale, and a small real-device set for touch, keyboard, camera, sensors, performance, and
browser chrome.

<Callout type="tip">
Record the model, OS, browser/app version, viewport and physical resolution, density, orientation,
input method, network, and whether the run was real, emulated, or simulated.
</Callout>

<Callout type="mistake">
Do not name a DevTools preset "tested on iPhone." It tested a viewport and some emulated properties.
Report it honestly, then schedule real hardware where touch, safe areas, keyboards, sensors, or resource
limits can change the outcome.
</Callout>

{/* IMAGE: a person holding a phone displaying two A/B test columns */}
<HotspotImage src="/notes/non-functional-testing-intro/compatibility/cross-device.jpg" alt="A person holding a smartphone that displays an A/B testing comparison" credit="Split Testing - Analysing AB Test Results on Mobile Device — Zuko.io Images, Wikimedia Commons, CC BY 2.0" creditHref="https://commons.wikimedia.org/wiki/File:Split_Testing_-_Analysing_AB_Test_Results_on_Mobile_Device.jpg" pins={[
  { x: 49, y: 51, label: "Physical display", desc: "Real density, brightness, safe areas, and browser chrome affect what the user can see and touch." },
  { x: 58, y: 57, label: "Thumb-held input", desc: "Touch targets, reach, gestures, and accidental activation cannot be inferred from a desktop pointer alone." },
  { x: 50, y: 36, label: "Content under real constraints", desc: "The critical journey must remain readable and operable when the virtual keyboard, rotation, or network changes available space." },
  { x: 81, y: 70, label: "Use context", desc: "Mobility, glare, interruptions, and connectivity make device context part of compatibility evidence." },
]}/>

<FlowAnimation title="From emulation breadth to real-device confidence" intervalMs={1600} nodes={[
  { label: "Use analytics and features to define device classes", desc: "Avoid brand collecting; choose characteristics tied to users and risk." },
  { label: "Sweep layouts quickly in responsive mode", desc: "Find overflow, breakpoints, and missing content across many widths." },
  { label: "Exercise controlled combinations in emulators or farms", desc: "Repeat OS, density, orientation, and network scenarios at scale." },
  { label: "Validate hardware-sensitive journeys on real devices", desc: "Touch, keyboards, camera, sensors, performance, safe areas, and browser chrome need physical evidence." },
]}/>

<CodePlayground language="python" title="A cross-device evidence oracle (Python)" code={`checks = {
    "small_touch_journey": True,
    "large_pointer_journey": True,
    "rotation_preserves_state": True,
    "real_device_validated": True,
}
for name, passed in checks.items(): print(name + "=" + ("PASS" if passed else "FAIL"))
result = "PASS" if all(checks.values()) else "FAIL"
assert result == "PASS", "device matrix rejected"
print("RESULT=" + result)`}/>

<CodePlayground language="java" title="A cross-device evidence oracle (Java)" code={`import java.util.LinkedHashMap;
import java.util.Map;
public class Main {
    public static void main(String[] args) {
        Map<String, Boolean> checks = new LinkedHashMap<>();
        checks.put("small_touch_journey", true);
        checks.put("large_pointer_journey", true);
        checks.put("rotation_preserves_state", true);
        checks.put("real_device_validated", true);
        boolean ok = true;
        for (var e : checks.entrySet()) { System.out.println(e.getKey() + "=" + (e.getValue() ? "PASS" : "FAIL")); ok &= e.getValue(); }
        String result = ok ? "PASS" : "FAIL";
        if (!result.equals("PASS")) throw new AssertionError("device matrix rejected");
        System.out.println("RESULT=" + result);
    }
}`}/>

<FirstTime title="Run an honest device-class check"><StepChecklist steps={[
  { text: "Choose classes from evidence", detail: "Use compact touch, large touch, and desktop pointer only when they represent supported users and risks." },
  { text: "Sweep widths and orientations", detail: "Check content, overflow, controls, and preserved state with responsive tools." },
  { text: "Select a hardware-sensitive journey", detail: "Exercise keyboard, touch, camera, sensor, safe area, or constrained network behavior." },
  { text: "Repeat it on a real device", detail: "Record the exact model and environment and compare with the emulated result." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "The layout works in Device Mode but the real phone hides the submit button.", fix: "Include browser chrome, safe areas, and the virtual keyboard in the real-device reproduction; test dynamic viewport units and scrolling behavior." },
  { symptom: "The matrix contains many nearly identical phones.", fix: "Group by risk-bearing characteristics—size, density, input, OS, performance, and feature hardware—then retain exact models only where evidence requires them." },
  { symptom: "Rotation reloads the page and loses the form.", fix: "Capture lifecycle and state behavior on real hardware, fix persistence or layout handling, and retest both directions during the critical journey." },
]}/>

<WhereToCheck>
- Analytics and supported-device policy, dated for the release.
- DevTools responsive mode for layout breadth; emulators and farms for controlled scale.
- Real hardware for touch, keyboard, sensors, camera, performance, safe areas, and browser chrome.
- [[non-functional-testing-intro/compatibility/responsive-checks]] for continuous-width layout checks.
</WhereToCheck>

<WorkedExample title="the keyboard that ate checkout">
1. Responsive mode at 390px shows the address form and button.
2. On a real phone, focusing the final field opens the virtual keyboard and the fixed footer covers the button.
3. The tester records model, OS, browser, orientation, viewport, and keyboard state.
4. The footer and scroll behavior are fixed; the flow is retested with real touch and keyboard transitions.
</WorkedExample>

<Quiz question="Which statement is accurate?" options={[
  "A device preset proves the named phone works", "Real devices make emulators useless", "Emulation gives fast controlled breadth; real devices validate hardware and context risks", "Screen width is the only device difference"
]} answer={2} explain="The tools answer different questions. Honest evidence labels simulation and reserves physical testing for differences it cannot reproduce faithfully." />

<Flashcards cards={[
  { front: "Device class", back: "A group defined by risk-bearing characteristics, not merely brand." },
  { front: "Emulation", back: "Fast, repeatable model of selected properties; not proof of physical-device behavior." },
  { front: "Real-device targets", back: "Touch, keyboards, sensors, camera, safe areas, browser chrome, performance, and network context." },
]}/>

<Challenge>Compare one critical journey in responsive mode and on a real phone. Record one difference the preset did not model.</Challenge>
<AskCommunity prompt="Which real-device behavior escaped your emulator or responsive-mode checks?" />

<Resources links={[
  { href: "https://developer.chrome.com/docs/devtools/device-mode/", title: "Chrome for Developers — Device Mode", kind: "read" },
  { href: "https://developer.android.com/studio/run/emulator", title: "Android Developers — Run apps on the Android Emulator", kind: "read" },
  { href: "https://www.youtube.com/watch?v=8oKu0njdnt8", title: "BrowserStack — How to test a .apk file using BrowserStack App Live", kind: "watch" },
]}/>
<Video href="https://www.youtube.com/watch?v=8oKu0njdnt8" title="How to test a .apk file using BrowserStack App Live" minutes={2} />

<Takeaways points={[
  "Choose device classes from supported users and risk-bearing characteristics.",
  "Label responsive simulation, emulation, cloud devices, and real hardware honestly.",
  "Use fast tools for breadth and real devices for physical input, hardware, context, and resource risks.",
  "Record the full device and environment so failures can be reproduced.",
]}/>
<Complete xp={10} />
