---
title: "Dashboards"
summary: "CI dashboards support decisions when they show trends and distributions for failure, duration, queueing, reruns, flakes, and coverage with clear scope, freshness, ownership, and drill-down."
tags: ["ci", "dashboards", "metrics", "analytics", "track-d"]
updated: "2026-07-17"
related:
  - "automation-in-cicd/scheduling-and-reporting/publishing-reports"
  - "automation-in-cicd/flake-management/detecting-flakes"
  - "automation-in-cicd/scheduling-and-reporting/notifications"
---

<Hook>
A dashboard says the pipeline is 96% successful. It mixes feature branches, canceled experiments,
nightly retries, and release builds. The number is precise, attractive, and useless for deciding
whether main is safe to ship.
</Hook>

<Callout type="analogy">
An aircraft panel does not show one giant “plane score.” Separate instruments expose speed, altitude,
heading, fuel, and engine state because each supports a different decision. A CI dashboard needs
separable measures, healthy ranges, trend context, and a route from a bad gauge to the underlying run.
</Callout>

<Term define="A CI dashboard is a time-scoped, filterable view of pipeline health derived from run, job, test, and deployment records. Useful measures include first-attempt success, final success, failure and cancellation rate, queue and execution duration distributions, flake/rerun rate, change-to-feedback latency, and evidence freshness. Every tile needs scope, denominator, time window, last updated time, owner, and drill-down.">CI dashboard</Term>

## Start with a decision, then choose a metric

| Decision | Better measure | Dangerous shortcut |
| --- | --- | --- |
| Can main ship? | latest required-gate result on current SHA | all-branch success rate |
| Is feedback slowing? | p50/p95 queue + execution duration | average total duration |
| Is the suite trustworthy? | first-attempt pass and rerun/flake rate | final green after retries |
| Where to invest? | top failure fingerprints × lost time | raw failure count |

Segment by pipeline source, branch, suite, environment, and period before comparing.

<Callout type="tip">
Show first-attempt and final outcomes side by side. The gap reveals hidden retries that a final-green
dashboard would otherwise erase.
</Callout>

<Callout type="mistake">
Treating coverage percentage or test count as quality. Both can rise while assertions weaken, risk
scope changes, or failures become less diagnosable. Pair activity/coverage measures with outcomes.
</Callout>

<HotspotImage src="/notes/automation-in-cicd/scheduling-and-reporting/dashboards.jpg" alt="A Curtiss C-46 aircraft cockpit with many gauges, controls, and two crew members" credit="C-46 cockpit — Bzuk, public domain" creditHref="https://commons.wikimedia.org/wiki/File:C-46_cockpit.jpg" pins={[
  { x: 42, y: 30, label: "Different gauges", desc: "Queue, duration, success, and flakes answer different questions; do not collapse them into one score." },
  { x: 70, y: 31, label: "Healthy ranges", desc: "A measure becomes actionable when the team defines expected bounds and ownership." },
  { x: 55, y: 58, label: "Controls", desc: "Filters for branch, source, environment, and time window change the interpretation." },
  { x: 83, y: 54, label: "Human decision", desc: "Dashboards assist an accountable operator; they do not replace context or investigation." },
]} />

<FlowAnimation title="From CI events to an honest dashboard" intervalMs={1500} nodes={[
  { label: "Events are emitted", desc: "Runs, jobs, attempts, tests, queues, and deployments get stable IDs." },
  { label: "Data is normalized", desc: "Status meanings, timestamps, branches, suites, and fingerprints align." },
  { label: "Scope is selected", desc: "Filter source, branch, environment, and time window." },
  { label: "Measures are calculated", desc: "Use explicit denominators and distributions, not ambiguous averages." },
  { label: "Freshness is shown", desc: "Last-updated and missing-data indicators prevent stale confidence." },
  { label: "Tile drills down", desc: "Every anomaly links to the exact runs, jobs, tests, and artifacts." },
]} />

<CodePlayground language="python" title="Run it — expose retries hidden by final success (Python)" code={```runs = [("r1", "failed", "passed"), ("r2", "passed", "passed"), ("r3", "failed", "passed")]
first_pass = sum(first == "passed" for _, first, _ in runs) / len(runs)
final_pass = sum(final == "passed" for _, _, final in runs) / len(runs)
print(f"first-attempt success: {first_pass:.0%}")
print(f"final success: {final_pass:.0%}")
print(f"retry-hidden gap: {final_pass - first_pass:.0%}")```} />

<CodePlayground language="java" title="Run it — expose retries hidden by final success (Java)" code={```import java.util.*;

public class Main {
    record Run(String id, String first, String last) {}
    public static void main(String[] args) {
        var runs = List.of(new Run("r1", "failed", "passed"),
            new Run("r2", "passed", "passed"), new Run("r3", "failed", "passed"));
        long first = runs.stream().filter(r -> r.first().equals("passed")).count();
        long last = runs.stream().filter(r -> r.last().equals("passed")).count();
        System.out.printf("first-attempt success: %.0f%%%n", 100.0 * first / runs.size());
        System.out.printf("final success: %.0f%%%n", 100.0 * last / runs.size());
        System.out.printf("retry-hidden gap: %.0f%%%n", 100.0 * (last - first) / runs.size());
    }
}```} />

<FirstTime title="Your mission: build one decision-driven view">
<StepChecklist steps={[
  { text: "Write the decision first", detail: "Name the user, decision, cadence, and action when the measure changes." },
  { text: "Define numerator and denominator", detail: "Specify included statuses, retries, branches, sources, and time window." },
  { text: "Add segmentation and freshness", detail: "Filter meaningful populations and expose stale/missing data." },
  { text: "Prove drill-down", detail: "Open the exact run and evidence behind one chart point." },
]} />
You now have a decision surface rather than a wall of vanity numbers.
</FirstTime>

<WhenItBreaks items={[
  { symptom: "Dashboard success disagrees with the release gate.", fix: "Compare SHA, branch, source, required-job scope, attempts, skipped/canceled treatment, and refresh time." },
  { symptom: "Average duration looks stable while developers complain.", fix: "Plot p50/p95 queue and execution separately; a few long waits disappear inside an average." },
  { symptom: "Flakes vanish after retries.", fix: "Store attempt-level outcomes and display first-attempt success, rerun rate, and final status together." },
  { symptom: "A chart changed after a taxonomy rename.", fix: "Version metric definitions, map old/new suite identifiers, and annotate breaks in historical series." },
]} />

<WhereToCheck>
- **Tile definition** — decision, formula, denominator, window, and owner.
- **Filters** — branch, source, environment, suite, and attempt.
- **Ingestion lag/freshness** — last event and missing-data state.
- **Raw run/job/test IDs** — trace a point back to evidence.
- **Metric-version history** — schema, status, or taxonomy changes.
</WhereToCheck>

<WorkedExample title="100% green hiding a 33% first-pass rate">
1. Three main-branch pipelines all finish green after automatic retries.
2. The dashboard counts only final status and reports 100% success.
3. Two runs failed first because of the same checkout timeout, costing 24 minutes each.
4. The team adds first-attempt success, rerun rate, failure fingerprint, and lost-time tiles.
5. The dashboard now exposes 33% first-pass success and points directly to the unstable dependency.
</WorkedExample>

<Quiz question="Which dashboard comparison best reveals hidden flakiness?" options={[
  "Total tests this month versus last month",
  "Final success rate alone",
  "First-attempt success versus final success after retries, segmented by suite",
  "Lines of test code versus product code"
]} answer={2} explain="Retry recovery creates a measurable gap between first and final outcome. Segmenting identifies where the instability lives." />

<Flashcards cards={[
  { front: "Denominator", back: "The population included in a rate; changing it changes the meaning." },
  { front: "p95 duration", back: "The value at or below which 95% of observations fall, exposing tail latency better than an average." },
  { front: "First-attempt success", back: "Share of runs green before retry; a trust signal for suite stability." },
  { front: "Freshness", back: "How recently the dashboard successfully incorporated source events." },
  { front: "Drill-down", back: "Path from aggregate metric to exact run, job, test, log, and artifact." },
]} />

<Challenge>
Take one existing CI success chart. Document its denominator, retries, skipped/canceled handling,
scope, freshness, and owner. Add first-attempt status and p95 latency, then trace one anomaly to raw evidence.
</Challenge>

<AskCommunity prompt="Dashboard metric [name] shows [value] but raw CI shows [difference]. Formula, denominator, filters, attempt handling, freshness, and example run IDs are [values].">
This makes a data-definition defect reproducible instead of debating screenshots.
</AskCommunity>

<Resources links={[
  { href: "https://docs.gitlab.com/user/analytics/ci_cd_analytics/", title: "GitLab Docs — CI/CD analytics", kind: "read" },
  { href: "https://docs.github.com/en/rest/actions/workflow-runs", title: "GitHub Docs — workflow runs API", kind: "read" },
]} />

<Video href="https://www.youtube.com/watch?v=qP8kir2GUgo" title="GitLab CI/CD Tutorial for Beginners — TechWorld with Nana" minutes={69} />

<Takeaways points={[
  "Begin with a decision and accountable user, then select measures.",
  "Expose scope, denominator, time window, attempt handling, freshness, and owner.",
  "Use distributions and segment meaningful populations instead of relying on averages.",
  "Compare first-attempt and final success to reveal retry-hidden instability.",
  "Every aggregate must drill down to exact runs and durable evidence.",
]} />

<Complete xp={10} />
