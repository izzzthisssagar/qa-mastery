---
title: "OS / versions"
summary: "Test operating-system and version boundaries with a dated support policy, upgrade paths, platform changes, and lifecycle evidence."
tags: ["non-functional-testing-intro", "compatibility", "track-c"]
updated: "2026-07-18"
related:
  - "non-functional-testing-intro/compatibility/cross-device"
  - "non-functional-testing-intro/compatibility/cross-browser"
  - "operating-systems-and-files/installing-and-managing-software/updates"
---

<Hook>
An application may work on a phone model today and fail after the same phone installs tomorrow's OS.
Permissions, storage, background work, certificates, keyboards, and system controls evolve underneath
the product. Compatibility is a moving boundary, not a sticker on the device.
</Hook>

<Callout type="analogy">
OS versions are editions of a rulebook. The field looks familiar, but a new edition can change how play
restarts, which equipment is permitted, or when a warning appears. Test the supported editions and the
moment teams move between them.
</Callout>

<Term define="OS-version compatibility testing evaluates supported operating-system releases and transitions, including clean install, update, upgrade, permissions, lifecycle, platform behavior changes, and minimum-version boundaries. Support claims must be dated because vendor releases and policies change.">OS-version compatibility</Term>

## Test boundaries and transitions

State a minimum supported version and a current range using product policy, audience data, store or
vendor requirements, and platform documentation. High-value rows are the minimum supported version,
latest stable version, upcoming beta when appropriate, and versions where a relevant behavior changed.
Test fresh install, app update, OS upgrade with existing data, permission changes, background/foreground,
notifications, links, files, and uninstall/reinstall behavior.

<Callout type="tip">
Subscribe to official Android, Apple, Microsoft, and browser platform change notes relevant to the
product. Convert each breaking or privacy-related change into an explicit scenario before release.
</Callout>

<Callout type="mistake">
"Supports Android" or "supports iOS" is not testable. Name version ranges and dates, and do not assume a
successful clean install covers upgrades with old databases, cached credentials, or previously granted
permissions.
</Callout>

{/* IMAGE: three phones displaying different Android Jelly Bean Easter-egg screens */}
<HotspotImage src="/notes/non-functional-testing-intro/compatibility/os-and-versions.jpg" alt="Three smartphones displaying different Android Jelly Bean Easter egg screens" credit="Android Jelly Bean — Galaxy Note 10 and Google, Wikimedia Commons, CC BY-SA 4.0" creditHref="https://commons.wikimedia.org/wiki/File:Android_Jelly_Bean.jpg" pins={[
  { x: 16, y: 45, label: "Older platform presentation", desc: "Minimum supported releases deserve explicit journey and performance coverage." },
  { x: 51, y: 45, label: "Same family, different behavior", desc: "Point releases and vendor builds can alter system UI, permissions, and lifecycle details." },
  { x: 84, y: 45, label: "Newer platform state", desc: "Latest releases expose behavior changes early; upcoming betas are risk signals, not production guarantees." },
  { x: 50, y: 84, label: "Upgrade path between versions", desc: "Preserved user data, permissions, sessions, and migrations are often riskier than a clean install." },
]}/>

<FlowAnimation title="A version boundary test" intervalMs={1600} nodes={[
  { label: "Publish a dated supported-version range", desc: "Make the claim precise enough to test and retire." },
  { label: "Read official behavior-change notes", desc: "Target permission, storage, lifecycle, privacy, and API changes relevant to the product." },
  { label: "Test clean install and upgrade paths", desc: "Preserve real prior state, data, permissions, and cached credentials." },
  { label: "Reassess on every platform release", desc: "Update the matrix and communicate deprecation before dropping support." },
]}/>

<CodePlayground language="python" title="An OS-version boundary oracle (Python)" code={`checks = {
    "minimum_version": True,
    "latest_stable": True,
    "upgrade_preserves_data": True,
    "permission_change_handled": True,
}
for name, passed in checks.items(): print(name + "=" + ("PASS" if passed else "FAIL"))
result = "PASS" if all(checks.values()) else "FAIL"
assert result == "PASS", "OS version boundary rejected"
print("RESULT=" + result)`}/>

<CodePlayground language="java" title="An OS-version boundary oracle (Java)" code={`import java.util.LinkedHashMap;
import java.util.Map;
public class Main {
    public static void main(String[] args) {
        Map<String, Boolean> checks = new LinkedHashMap<>();
        checks.put("minimum_version", true);
        checks.put("latest_stable", true);
        checks.put("upgrade_preserves_data", true);
        checks.put("permission_change_handled", true);
        boolean ok = true;
        for (var e : checks.entrySet()) { System.out.println(e.getKey() + "=" + (e.getValue() ? "PASS" : "FAIL")); ok &= e.getValue(); }
        String result = ok ? "PASS" : "FAIL";
        if (!result.equals("PASS")) throw new AssertionError("OS version boundary rejected");
        System.out.println("RESULT=" + result);
    }
}`}/>

<FirstTime title="Test one version transition"><StepChecklist steps={[
  { text: "Record the dated support range", detail: "Identify the minimum, latest stable, and the platform change that threatens the feature." },
  { text: "Create meaningful prior state", detail: "Install the previous release, sign in, create data, set permissions, and background the app." },
  { text: "Upgrade the OS or app", detail: "Preserve state and observe migrations, permissions, notifications, links, and lifecycle." },
  { text: "Run the critical journey and recovery", detail: "Verify data integrity, error handling, and behavior after restart." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "A clean install passes but upgraded users crash.", fix: "Reproduce with the old schema and cached state, inspect migrations, and retain upgrade fixtures in regression coverage." },
  { symptom: "Permissions silently reset after an OS update.", fix: "Test denied, granted, one-time, and changed permission states; explain recovery instead of assuming prior grants persist." },
  { symptom: "Nobody knows whether an old version is supported.", fix: "Create an owner-approved, dated version policy and map each promised row to evidence and deprecation communication." },
]}/>

<WhereToCheck>
- Official platform release notes and behavior-change documentation.
- App/store minimum requirements and internal supported-version policy.
- Install, update, OS-upgrade, migration, permission, lifecycle, notification, and deep-link paths.
- Crash and analytics segmentation by exact OS and app version.
</WhereToCheck>

<WorkedExample title="photos permission changed under the app">
1. A photo uploader passed on the team's current OS with broad library access.
2. The next platform version introduces a more limited permission state; upgraded users select a photo
   but the app assumes the entire library remains readable.
3. The tester reproduces with preserved permissions and existing draft data, then records exact versions.
4. The app handles limited access and offers a clear recovery path; clean and upgrade flows are retested.
</WorkedExample>

<Quiz question="Which scenario adds the most version-specific evidence?" options={[
  "Only a clean install on the newest OS", "An upgrade from a supported old version with data and permissions preserved", "A screenshot of the home page", "A generic claim that Android works"
]} answer={1} explain="Transitions carry schemas, sessions, permissions, caches, and user data across changing platform rules, exposing risks a clean install cannot." />

<Flashcards cards={[
  { front: "Version claim", back: "A dated range with minimum and current releases, not a platform name alone." },
  { front: "Boundary rows", back: "Minimum supported, latest stable, relevant behavior-change versions, and beta when justified." },
  { front: "Upgrade evidence", back: "Prior app data, sessions, permissions, caches, migrations, and post-upgrade critical journeys." },
]}/>

<Challenge>Take one official platform behavior change and design clean-install, upgrade, denied-permission, and recovery scenarios.</Challenge>
<AskCommunity prompt="Which OS upgrade exposed a defect that your clean-install testing missed?" />

<Resources links={[
  { href: "https://developer.android.com/about/versions", title: "Android Developers — Platform releases", kind: "read" },
  { href: "https://developer.apple.com/documentation/ios-ipados-release-notes", title: "Apple Developer — iOS and iPadOS release notes", kind: "read" },
  { href: "https://www.youtube.com/watch?v=82einkyFns4", title: "Android Developers — Testing platform changes in Android 11", kind: "watch" },
]}/>
<Video href="https://www.youtube.com/watch?v=82einkyFns4" title="Testing platform changes in Android 11" minutes={4} />

<Takeaways points={[
  "Make OS support precise and dated because the boundary moves.",
  "Target minimum, latest, and behavior-change versions rather than every release uniformly.",
  "Upgrade paths with preserved state reveal risks clean installs miss.",
  "Turn official platform changes into explicit permission, lifecycle, storage, and recovery scenarios.",
]}/>
<Complete xp={10} />
