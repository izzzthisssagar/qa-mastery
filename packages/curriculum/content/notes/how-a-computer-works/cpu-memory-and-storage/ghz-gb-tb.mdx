---
title: "GHz, GB, TB"
summary: "The alphabet soup on every spec sheet, decoded — what the numbers measure, which ones matter, and how to read a laptop ad without being fooled."
tags: ["computer-basics", "hardware", "track-a"]
updated: "2026-07-09"
---

<Hook>
"Intel i5, 3.3 GHz, 8 GB RAM, 512 GB SSD." To most people that's a spell in Latin.
To a salesperson, it's an opportunity. After this topic, it's just... a sentence you
read. And the next time someone tries to sell your uncle a laptop with "huge 512
memory", you'll be able to lean in and ruin the salesman's whole afternoon.
</Hook>

<Callout type="analogy">
Spec numbers are the **nutrition label of a computer**. Calories, protein, sugar —
each measures a DIFFERENT thing, and no single number means "healthy". GHz measures
speed-per-chef, GB measures counter or pantry space (crucially different!), and the
sticker price measures none of them. Read the whole label or get fooled by the front
of the box — same rule as the supermarket.
</Callout>

## GHz — how fast each chef chops

<HotspotImage
  src="/notes/how-a-computer-works/cpu-top.jpg"
  alt="An Intel Core i5 CPU with 3.30GHz printed on its lid, in front of its retail box"
  credit="Photo: Wikimedia Commons, CC0"
  creditHref="https://commons.wikimedia.org/wiki/File:INTEL_SKYLAKE_CPU_CORE_i5_Skylake.jpg"
  pins={[
    { x: 36, y: 57, label: "'3.30GHZ' — right on the chip", desc: "3.30 gigahertz = 3.3 BILLION cycles per second. Each cycle is one beat of the fetch-decode-execute dance from two topics ago. It's printed on the chip like an athlete's personal best." },
    { x: 76, y: 63, label: "'i5-6600' — the model name", desc: "Intel's naming: i3 < i5 < i7 < i9, roughly budget → beast. The 4-digit number is the generation and model. Model name usually tells you more about real-world speed than GHz alone — a modern i5 beats an ancient i7." },
    { x: 78, y: 71, label: "'LGA1151' — the socket type", desc: "The exact socket this chip fits (remember the pin grid?). Sockets change every few years — the reason you usually can't drop a new CPU into an old motherboard. Compatibility: hardware edition." },
  ]}
/>

**GHz = billions of cycles per second.** 3.3 GHz = 3.3 billion beats of the
fetch-decode-execute dance, per chef, per second.

The catch everyone misses: **GHz only compares chefs of the same generation.** A
modern 3 GHz chip demolishes a 3.5 GHz chip from 2010, because each modern cycle
does more work. It's chopping speed, not cooking skill — and a 2026 chef with a
sharp knife beats a 2010 chef with a slightly faster arm.

## GB — how big the counter and the pantry are

The size ladder, each step ×1000 (close enough):

- **KB (kilobyte)** — a paragraph of text. This sentence is a fraction of one.
- **MB (megabyte)** — a photo (2–5 MB), a song (3–10 MB), an app download.
- **GB (gigabyte)** — a movie (1–4 GB), a big game (50+ GB), RAM sizes (8/16/32 GB), phone storage (128/256 GB).
- **TB (terabyte)** — 1000 GB. External drives, big laptop disks, your entire digital life with room to spare.

The trap from last topic, now in numbers: **"8 GB RAM" and "512 GB SSD" are both
"GB" and have NOTHING to do with each other.** One is the counter, one is the
pantry. A machine with a huge pantry and a tiny counter still chokes on 30 browser
tabs — while the salesman points proudly at the 512.

<Callout type="tip">
Real spec-reading, tester edition: the numbers form a SYSTEM, and the slowest part
wins. Monster CPU + 4 GB RAM = the chefs wait on a cramped counter. Huge RAM + old
HDD = everything loads like it's being faxed in. When a "slow laptop" bug arrives,
the spec line tells you where the system's ceiling PROBABLY is — before you run a
single test. That's why every bug template asks for it.
</Callout>

<FirstTime title="Your mission: read your own nutrition label">
<StepChecklist
  steps={[
    { text: "Find your CPU's GHz and model name", detail: "Windows: Settings → System → About (or Task Manager → Performance → CPU). Mac: About This Mac. Note BOTH — model name and GHz." },
    { text: "Find your RAM in GB", detail: "Same screen. This is the counter. 8 GB = normal life, 16 GB = comfortable, 32 GB = someone who never closes tabs." },
    { text: "Find your storage in GB/TB", detail: "Settings → Storage / About This Mac → Storage. The pantry. Note total AND free." },
    { text: "Write the full spec line", detail: "Like a pro: 'i5-6600 @ 3.3 GHz, 16 GB RAM, 512 GB SSD (40% free)'. This exact sentence appears in thousands of real bug reports every day." },
    { text: "Reality-check a laptop ad", detail: "Find any laptop ad online. Locate the GHz, the RAM GB, the storage GB. Notice which number the ad shouts loudest... and which it whispers. (It always whispers the RAM.)" },
  ]}
/>
You just read a spec sheet like a professional and audited an ad like a skeptic.
Both skills compound.
</FirstTime>

<WhenItBreaks
  items={[
    {
      symptom: "I bought a laptop with 'huge memory' (512 GB!) and it still lags with many tabs open.",
      fix: "The 512 was the pantry (storage). Tab-juggling lives on the counter (RAM) — and that machine probably has 4–8 GB of it. The ad wasn't lying, it was just... aiming your attention. Check RAM before buying, not after. (Too late for this laptop; close some tabs and spread the wisdom.)",
    },
    {
      symptom: "The download says it needs 4 GB but I have a 3 GB file limit warning — units are melting my brain.",
      fix: "Slow down and read the letters, not just numbers: MB vs GB vs TB are ×1000 apart. A 4 GB requirement vs 3 GB FREE SPACE means clear one movie's worth of pantry. Half of all 'not enough space' confusion is a misread letter. The letter is the number.",
    },
    {
      symptom: "My friend's 2.8 GHz laptop is faster than my 3.4 GHz one. GHz is a lie?!",
      fix: "GHz compares within a generation only. Their modern 2.8 does more work per cycle than your older 3.4 — sharper knife beats faster arm. Also check the rest of the system: their SSD vs your HDD alone could explain everything. Single numbers never tell the whole story; systems do.",
    },
    {
      symptom: "Phone says storage full but I 'only have a few apps'.",
      fix: "Photos and videos are the silent pantry-eaters — 4K video swallows a GB every few minutes. Check the storage breakdown (Settings shows exactly what's eating what), offload the camera roll to cloud/computer, and watch 50 GB reappear. The few apps were never the problem.",
    },
  ]}
/>

<WhereToCheck>
Every number on the nutrition label is self-reported and one click away:

- **The whole label at once:** Windows Settings → System → About / Mac About This Mac. CPU + GHz + RAM on one screen.
- **Storage detail:** Settings → Storage — with the what's-eating-it breakdown.
- **Live speeds:** Task Manager → Performance shows the CPU's CURRENT GHz too — it idles slow to save power and sprints under load. Watch it change. The athlete stretches AND sprints.

Cross-check ads against reality: the machine itself never lies about its own specs.
Salespeople… let's say the machine is the primary source. Primary sources —
another QA habit, arriving early.
</WhereToCheck>

<Callout type="mistake">
Comparing computers by ONE number — any one number. GHz alone, RAM alone, price
alone: all three mislead in different ways. The system is a kitchen: chefs (CPU),
counter (RAM), pantry (storage) — judge the kitchen, not the shiniest knife. The
same mistake will reappear in your QA career as "the app is fast because THIS page
loads quickly" — one measurement, system-wide claim. It's wrong there too.
</Callout>

<FlowAnimation
  title="Climb the units ladder — press Play"
  intervalMs={1400}
  nodes={[
    { label: "KB", desc: "A kilobyte — a paragraph of text. This sentence is a fraction of one. The smallest step you'll talk about daily." },
    { label: "MB", desc: "×1000: a megabyte — one photo (2–5 MB), one song (3–10 MB), an app download. The currency of files." },
    { label: "GB", desc: "×1000 again: a gigabyte — a movie, a big game's slice, RAM sizes (8/16/32), phone storage (128/256). Where counter and pantry BOTH live — and get confused." },
    { label: "TB", desc: "×1000 once more: a terabyte = 1000 GB — big drives, your entire digital life with room left over. Above this live the data centers." },
  ]}
/>

<CodePlayground
  language="python"
  title="Try it — build a size translator"
  code={`# Turn raw bytes into human-speak — the exact logic every OS uses for file sizes.
def human(size_bytes):
    for unit in ["B", "KB", "MB", "GB", "TB"]:
        if size_bytes < 1024:
            return f"{size_bytes:.1f} {unit}"
        size_bytes /= 1024
    return f"{size_bytes:.1f} PB"

for n in [512, 2_048_000, 4_294_967_296, 1_099_511_627_776]:
    print(f"{n:>16} bytes = {human(n)}")
print("Change the numbers — try your phone's storage in bytes.")`}
/>

<WorkedExample title="reading a real laptop ad like a spec sheet">
An actual marketplace listing: *"POWERFUL laptop!! Intel i7, HUGE 1TB storage, super fast!!"* The audit:

1. **What's shouted:** 1 TB storage (the pantry) and "i7" (the badge). What's whispered: no RAM figure anywhere, no <Term define="The chip's generation — a modern i5 beats an ancient i7. The 4-digit model number tells you the year, roughly.">generation</Term> on the i7.
2. **Dig:** the seller confirms on request: i7-2600 (from 2011!) and 4 GB RAM.
3. **Translate:** a 14-year-old chef, a cramped counter, a huge pantry. It will store movies beautifully and choke on 15 browser tabs.
4. **Verdict:** the ad was technically true and practically misleading — the classic pattern. Buyer saved by three questions that took two minutes. Uncle protected. Salesman's afternoon: ruined.
</WorkedExample>

<Video href="https://www.youtube.com/watch?v=X7yWZ4tvKPE" title="Techquickie — what GHz and cores mean for real speed" minutes={5} />

<Quiz
  question="A laptop ad shouts: 'BLAZING 512 GB!' A user asks if it'll handle their 40 open browser tabs smoothly. What do you check?"
  options={[
    "Nothing — 512 GB is plenty for tabs",
    "The RAM (GB) — tabs live on the counter, and the shouted 512 is the pantry",
    "The GHz — tabs are about CPU speed only",
    "The screen resolution",
  ]}
  answer={1}
  explain="Open tabs live in RAM. The advertised 512 GB is storage — irrelevant to tab-juggling. This exact confusion sells underpowered laptops daily. Also enjoy the pattern: the ad shouts the biggest number, not the most relevant one. Reading past that is a life skill AND a testing skill."
/>

<Flashcards
  cards={[
    { front: "GHz", back: "Billions of CPU cycles per second — chopping speed per chef. Only comparable within the same chip generation: sharp knife beats fast arm." },
    { front: "KB → MB → GB → TB", back: "×1000 each step. Paragraph → photo/song → movie/RAM sizes → your whole digital life. The letter IS the number." },
    { front: "8 GB RAM vs 512 GB SSD", back: "Counter vs pantry — same unit, unrelated jobs. Tabs need the 8; downloads need the 512. Ads shout the 512." },
    { front: "i3/i5/i7/i9", back: "Intel's ladder, budget → beast. Model name + generation usually beats raw GHz for judging real speed." },
    { front: "The spec line", back: "'CPU @ GHz, X GB RAM, Y GB SSD (Z% free)' — the standard environment sentence in real bug reports. You can now write yours." },
  ]}
/>

<Challenge>
Do the uncle-protection drill for real: find one laptop ad online, write down its
CPU/GHz, RAM and storage, and give a one-sentence verdict — "good for X, will
struggle with Y." Example: "great pantry, cramped counter — fine for movies, will
cry at 30 tabs." You can now do in one minute what most buyers can't do at all. Use
this power for good (or at least for family WhatsApp).
</Challenge>

<AskCommunity prompt="Looking at a machine with [CPU @ GHz, X GB RAM, Y storage]. I mainly do [tabs/videos/coding/games]. Where's the bottleneck for my use — or is this balanced?">
Spec questions are the friendliest questions in tech — everyone loves judging a
laptop. Bring the three numbers + your actual use case, and you'll get sharper
advice than any store. Notice: numbers + context + expected use = an answerable
question. The same formula makes bugs answerable too.
</AskCommunity>

<Resources
  links={[
    { href: "https://www.youtube.com/watch?v=X7yWZ4tvKPE", title: "Techquickie — what do GHz and cores actually mean for speed?", kind: "video" },
    { href: "https://edu.gcfglobal.org/en/computerbasics/understanding-the-cloud/1/", title: "GCFGlobal — computer basics series (units and beyond)", kind: "read" },
    { href: "https://www.howtogeek.com/177645/htg-explains-why-you-cant-compare-cpus-based-on-clock-speed-alone/", title: "How-To Geek — why GHz alone can't compare CPUs", kind: "read" },
  ]}
/>

<Takeaways
  points={[
    "GHz = billions of cycles/second per chef — comparable only within a generation. Sharp knife beats fast arm.",
    "KB→MB→GB→TB, ×1000 per step. Misreading the letter causes half of all space confusion.",
    "RAM GB and storage GB share a unit and nothing else. Ads shout the pantry; performance lives on the counter.",
    "Judge the kitchen, not one number — the slowest part sets the speed. (Same trap exists in performance testing.)",
    "Your spec line — CPU @ GHz, RAM, storage, % free — is standard bug-report furniture. Write it once, reuse forever.",
  ]}
/>

<Complete xp={10} />
