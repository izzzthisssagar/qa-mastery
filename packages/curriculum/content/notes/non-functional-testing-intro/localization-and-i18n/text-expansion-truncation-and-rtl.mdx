---
title: "Text expansion, truncation & RTL"
summary: "Stress layouts with expanded strings and bidirectional content, then verify direction, order, isolation, focus, icons, and readable truncation."
tags: ["non-functional-testing-intro", "localization-and-i18n", "track-c"]
updated: "2026-07-18"
related:
  - "non-functional-testing-intro/localization-and-i18n/i18n-vs-l10n-in-plain-words"
  - "non-functional-testing-intro/localization-and-i18n/pseudo-localization-tricks"
  - "non-functional-testing-intro/compatibility/responsive-checks"
---

<Hook>
The English button "Save" is a tiny suitcase. A translation may need a trunk. Meanwhile Arabic and
Hebrew do more than align the suitcase to the other side: text flows right-to-left while embedded phone
numbers, codes, and Latin names keep their own direction. Layout and text direction must cooperate.
</Hook>

<Callout type="analogy">
Bidirectional text is a two-way railway sharing one station. Each train keeps its travel direction, but
platform order and signs must prevent passengers from boarding the wrong sequence. Guessing with spaces
or reversing strings is like rearranging carriages by hand.
</Callout>

<Term define="Text expansion is the increase in space a localized string needs. Right-to-left (RTL) support handles scripts whose base direction is RTL while correctly isolating embedded left-to-right segments such as numbers, URLs, and product codes. Direction is semantic behavior, not merely right alignment.">Text expansion and RTL support</Term>

## Stress length and direction separately

Use real long translations and pseudo-localization to expose fixed widths, clipped controls, overlapping
badges, and inaccessible ellipses. For RTL, set document or component direction correctly, prefer
logical CSS properties, mirror directional icons where meaning reverses, and keep universal symbols
unchanged. Check DOM reading order, keyboard focus, form fields, tables, breadcrumbs, mixed numbers,
punctuation, phone numbers, URLs, and copied text. Follow the Unicode Bidirectional Algorithm and W3C
guidance instead of manually reversing strings.

<Callout type="tip">
Test a string that combines Arabic or Hebrew, a Latin product name, punctuation, and a numeric ID. Mixed
content reveals ordering and isolation defects that monolingual sample text cannot.
</Callout>

<Callout type="mistake">
Do not implement RTL with `text-align: right` or by reversing characters. Alignment does not change
semantic direction, and reversal corrupts shaping, numbers, punctuation, cursor movement, selection,
copy/paste, and assistive-technology output.
</Callout>

{/* IMAGE: a large multilingual sign with Arabic, English, Urdu, and Malay lines and directional arrows */}
<HotspotImage src="/notes/non-functional-testing-intro/localization-and-i18n/text-expansion-truncation-and-rtl.jpg" alt="A multilingual information sign with Arabic, English, Urdu, and Malay text above two arrows" credit="Roza Rasool Information Sign — HolyArtThou, Wikimedia Commons, CC BY-SA 4.0" creditHref="https://commons.wikimedia.org/wiki/File:Roza_Rasool_Information_Sign.jpg" pins={[
  { x: 52, y: 30, label: "Arabic RTL line", desc: "Base direction, shaping, punctuation, and embedded marks must be interpreted semantically." },
  { x: 52, y: 43, label: "Short English line", desc: "English length is not a safe container specification for other localized strings." },
  { x: 52, y: 57, label: "Longer Malay line", desc: "Different translations occupy different widths and heights; containers must flex without collision." },
  { x: 52, y: 79, label: "Directional arrows", desc: "Mirror icons only when their meaning is directional; test that text order and navigation still match." },
]}/>

<FlowAnimation title="A length-and-direction stress pass" intervalMs={1600} nodes={[
  { label: "Expand every translatable message", desc: "Accents and padding reveal hard-coded strings and rigid containers early." },
  { label: "Switch the base direction to RTL", desc: "Use semantic direction and logical layout properties, not alignment hacks." },
  { label: "Mix scripts, numbers, punctuation, and codes", desc: "Isolation problems appear where directional runs meet." },
  { label: "Verify visual, keyboard, copy, and spoken order", desc: "A screenshot alone cannot prove focus, selection, or assistive-technology correctness." },
]}/>

<CodePlayground language="python" title="A length-and-direction oracle (Python)" code={`checks = {
    "expanded_text_visible": True,
    "rtl_order_correct": True,
    "mixed_id_isolated": True,
    "focus_order_meaningful": True,
}
for name, passed in checks.items(): print(name + "=" + ("PASS" if passed else "FAIL"))
result = "PASS" if all(checks.values()) else "FAIL"
assert result == "PASS", "length/direction oracle rejected"
print("RESULT=" + result)`}/>

<CodePlayground language="java" title="A length-and-direction oracle (Java)" code={`import java.util.LinkedHashMap;
import java.util.Map;
public class Main {
    public static void main(String[] args) {
        Map<String, Boolean> checks = new LinkedHashMap<>();
        checks.put("expanded_text_visible", true);
        checks.put("rtl_order_correct", true);
        checks.put("mixed_id_isolated", true);
        checks.put("focus_order_meaningful", true);
        boolean ok = true;
        for (var e : checks.entrySet()) { System.out.println(e.getKey() + "=" + (e.getValue() ? "PASS" : "FAIL")); ok &= e.getValue(); }
        String result = ok ? "PASS" : "FAIL";
        if (!result.equals("PASS")) throw new AssertionError("length/direction oracle rejected");
        System.out.println("RESULT=" + result);
    }
}`}/>

<FirstTime title="Run a mixed-direction stress check"><StepChecklist steps={[
  { text: "Enable an expanded pseudo-locale", detail: "Visit navigation, buttons, tables, dialogs, toasts, errors, and small-screen states." },
  { text: "Switch to a real RTL locale", detail: "Verify semantic direction, logical spacing, layout order, and directional icons." },
  { text: "Add mixed content", detail: "Use an RTL phrase with a Latin product name, punctuation, phone number, and order ID." },
  { text: "Test interaction order", detail: "Tab, select, edit, copy/paste, and listen with a supported screen reader." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "An expanded button clips its final word.", fix: "Remove fixed dimensions, allow wrapping or an approved flexible pattern, and retest at narrow widths and zoom." },
  { symptom: "An order ID appears on the wrong side of punctuation in Arabic.", fix: "Apply correct base direction and isolate the embedded directional run using suitable HTML/Unicode mechanisms from W3C guidance." },
  { symptom: "The RTL page looks mirrored but tab order is confusing.", fix: "Inspect DOM order and focus sequence; visual CSS reordering must not create a different semantic journey." },
]}/>

<WhereToCheck>
- Navigation, compact buttons, tables, breadcrumbs, badges, dialogs, toasts, and validation.
- Logical CSS properties, `dir`, `bdi`, `bdo`, and Unicode bidi isolation where appropriate.
- Mixed-script identifiers, phone numbers, URLs, punctuation, and input cursor movement.
- Keyboard focus, selection, copy/paste, and supported screen-reader output.
</WhereToCheck>

<WorkedExample title="the reversed-looking order number">
1. Arabic confirmation text embeds Latin product `QA-Pro` and order `AB-1204`.
2. Without isolation, punctuation and the ID appear in a confusing visual order; copying produces an
   unexpected sequence.
3. The tester records rendered, copied, and accessibility-tree evidence—not just alignment.
4. The inline runs are isolated semantically; Arabic, English, keyboard, and screen-reader paths are retested.
</WorkedExample>

<Quiz question="Which approach correctly supports RTL?" options={[
  "Reverse every string", "Right-align the page", "Set semantic direction, use logical layout properties, and isolate mixed-direction runs", "Insert spaces until punctuation looks right"
]} answer={2} explain="RTL is a Unicode and layout behavior. Semantic direction and isolation preserve shaping, numbers, interaction, copying, and assistive-technology interpretation." />

<Flashcards cards={[
  { front: "Expansion test", back: "Use real long strings or pseudo-locales to expose rigid containers and hidden content." },
  { front: "RTL is not", back: "Right alignment or reversed characters." },
  { front: "Mixed-direction stress", back: "RTL text plus Latin names, numbers, punctuation, URLs, and identifiers." },
]}/>

<Challenge>Test one compact form with expanded strings and an RTL mixed-script order ID; verify pixels, focus, selection, copy, and spoken order.</Challenge>
<AskCommunity prompt="Which mixed-direction string exposed an RTL defect that monolingual samples missed?" />

<Resources links={[
  { href: "https://www.w3.org/International/articles/inline-bidi-markup/", title: "W3C Internationalization — Inline Bidi Markup", kind: "read" },
  { href: "https://www.w3.org/International/questions/qa-html-dir", title: "W3C Internationalization — Structural markup and right-to-left text", kind: "read" },
  { href: "https://www.youtube.com/watch?v=rBK9CNIZWLY", title: "The Unicode Consortium — Bidirectional Text (Part 3): Mastering Bidirectional Content for Translators and Localizers", kind: "watch" },
]}/>
<Video href="https://www.youtube.com/watch?v=rBK9CNIZWLY" title="Bidirectional Text (Part 3):  Mastering Bidirectional Content for Translators and Localizers" minutes={56} />

<Takeaways points={[
  "Stress both text length and semantic direction; they expose different defect classes.",
  "RTL requires correct direction, logical layout, and mixed-run isolation—not alignment or reversal.",
  "Test long content at narrow widths and zoom without hiding meaning behind truncation.",
  "Verify focus, selection, copy, and spoken order as well as screenshots.",
]}/>
<Complete xp={10} />
