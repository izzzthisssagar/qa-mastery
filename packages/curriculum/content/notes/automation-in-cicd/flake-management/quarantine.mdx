---
title: "Quarantine"
summary: "Quarantine temporarily removes a proven unstable test from a blocking lane while keeping it visible, running, owned, time-bounded, and measured; an untracked skip is deletion, not quarantine."
tags: ["ci", "flaky-tests", "quarantine", "governance", "track-d"]
updated: "2026-07-17"
related:
  - "automation-in-cicd/flake-management/detecting-flakes"
  - "automation-in-cicd/flake-management/retries"
  - "automation-in-cicd/gitlab-ci-and-quality-gates/blocking-a-merge-on-failure"
---

<Hook>
A flaky checkout test blocks releases, so someone adds `skip`. Six months later checkout breaks in
production and the team discovers its only end-to-end guard has not executed once. The immediate
noise disappeared by silently deleting coverage.
</Hook>

<Callout type="analogy">
Medical quarantine separates a risk from the main population while preserving identification,
observation, authority, and a condition for release. CI quarantine should isolate blocking impact,
not make the test invisible.
</Callout>

<Term define="Test quarantine is a temporary governance state for a confirmed unstable test. The test is excluded from a specific blocking decision but continues in a visible non-blocking lane with an owner, issue, reason, evidence, start date, expiry, review cadence, affected risk, and objective exit criteria. Quarantine volume and age are quality metrics.">Test quarantine</Term>

## A quarantine record is part of the test

```yaml
test_id: checkout::creates_order
issue: QA-1842
owner: payments-qa
started: 2026-07-17
expires: 2026-07-24
scope: chromium-linux
exit: 200 consecutive equivalent passes after fix
```

Keep the quarantined lane red/visible in reports without blocking the chosen merge/release decision.
If the feature risk is critical, replace lost coverage with another gate before relaxing the original.

<Callout type="tip">
Expire quarantine automatically. A bot or gate should fail governance when the expiry passes, even
if the test itself remains non-blocking, forcing review instead of permanent limbo.
</Callout>

<Callout type="mistake">
Quarantining after one unexplained failure. First preserve evidence and establish instability. A
deterministic product regression placed in quarantine is a safety failure.
</Callout>

<HotspotImage src="/notes/automation-in-cicd/flake-management/quarantine.jpg" alt="Historical orange polio quarantine placard with public-health order text" credit="Polio quarantine card — author unknown, U.S. public domain" creditHref="https://commons.wikimedia.org/wiki/File:Polio_quarantine_card.jpg" pins={[
  { x: 50, y: 24, label: "Named condition", desc: "The sign clearly identifies why isolation exists; test quarantine needs the same explicit reason." },
  { x: 50, y: 57, label: "Governance", desc: "Authority, scope, evidence, and enforcement rules prevent informal permanent skipping." },
  { x: 24, y: 78, label: "Owner", desc: "A person or team must accept responsibility for diagnosis and restoration." },
  { x: 73, y: 78, label: "Exit", desc: "Expiry and measurable release criteria return the test to the blocking lane." },
]} />

<FlowAnimation title="A controlled quarantine lifecycle" intervalMs={1500} nodes={[
  { label: "Instability is evidenced", desc: "Attempt history proves nondeterminism or environment instability." },
  { label: "Risk is assessed", desc: "Decide whether alternate coverage makes temporary non-blocking status acceptable." },
  { label: "Record is approved", desc: "Owner, issue, scope, reason, start, expiry, and exit criteria." },
  { label: "Separate lane keeps running", desc: "Results remain visible and measured without blocking the chosen decision." },
  { label: "Cause is fixed", desc: "Change test, product, data, environment, or infrastructure." },
  { label: "Exit evidence restores gate", desc: "Meet stability threshold and remove quarantine metadata." },
]} />

<CodePlayground language="python" title="Run it — flag expired quarantine records (Python)" code={```from datetime import date
records = [("checkout", date(2026, 7, 16)), ("search", date(2026, 7, 20))]
today = date(2026, 7, 17)
for test, expiry in records:
    print(test, "EXPIRED" if expiry < today else f"active until {expiry}")```} />

<CodePlayground language="java" title="Run it — flag expired quarantine records (Java)" code={```import java.time.*;
import java.util.*;

public class Main {
    record Q(String test, LocalDate expiry) {}
    public static void main(String[] args) {
        var today = LocalDate.parse("2026-07-17");
        var records = List.of(new Q("checkout", LocalDate.parse("2026-07-16")),
            new Q("search", LocalDate.parse("2026-07-20")));
        for (Q q : records)
            System.out.println(q.test() + " " + (q.expiry().isBefore(today) ? "EXPIRED" : "active until " + q.expiry()));
    }
}```} />

<FirstTime title="Your mission: quarantine without losing the risk">
<StepChecklist steps={[
  { text: "Prove and classify instability", detail: "Link attempt evidence and distinguish test, product, environment, infrastructure, or dependency." },
  { text: "Assess lost coverage", detail: "Name the protected risk and any replacement gate." },
  { text: "Create the governance record", detail: "Owner, issue, reason, scope, start, expiry, review, and exit threshold." },
  { text: "Keep it executing", detail: "Verify the separate lane reports results and expiry monitoring works." },
]} />
You have temporary isolation with accountability, not a permanent skip.
</FirstTime>

<WhenItBreaks items={[
  { symptom: "Quarantined tests disappear from reports.", fix: "Run them in a separate non-blocking job and publish attempt-level results and artifacts." },
  { symptom: "The quarantine list only grows.", fix: "Set short expiries, age/volume limits, weekly review, owners, and a governance failure for overdue records." },
  { symptom: "A real regression was quarantined.", fix: "Restore blocking behavior, reassess release impact, and require instability evidence before future approval." },
  { symptom: "The test passes but remains quarantined.", fix: "Evaluate objective exit criteria across representative conditions, then remove metadata and confirm the gate." },
]} />

<WhereToCheck>
- **Quarantine registry/annotation** — issue, owner, scope, dates, and exit.
- **Non-blocking job report** — proof the test still executes.
- **Coverage/risk record** — what gate was weakened and its substitute.
- **Expiry monitor** — overdue state and escalation.
- **Attempt history after fix** — evidence required for restoration.
</WhereToCheck>

<WorkedExample title="isolating one browser without hiding checkout">
1. Checkout is stable on Chromium/Firefox but intermittently times out on WebKit Linux.
2. Evidence ties the failure to a WebKit runner image update; product API results are correct.
3. The team quarantines only `webkit-linux`, not the entire test, for seven days.
4. Chromium/Firefox remain release-blocking; WebKit runs in a visible job linked to the issue.
5. After the runner fix and 200 passes, expiry automation restores WebKit to the gate.
</WorkedExample>

<Quiz question="Which action is a real quarantine?" options={[
  "Delete the flaky test",
  "Skip it indefinitely with a comment",
  "Run it visibly outside one blocking decision with owner, issue, expiry, risk assessment, and exit criteria",
  "Retry until green and report only final status"
]} answer={2} explain="Quarantine is temporary, scoped, observable, owned, and reversible. Silent or indefinite exclusion is lost coverage." />

<Flashcards cards={[
  { front: "Quarantine", back: "Temporary scoped removal from a blocking decision while execution and visibility continue." },
  { front: "Expiry", back: "Date that forces review or restoration instead of permanent limbo." },
  { front: "Exit criteria", back: "Objective evidence required to return the test to its blocking lane." },
  { front: "Risk substitution", back: "Alternate control protecting the behavior while original coverage is non-blocking." },
  { front: "Quarantine debt", back: "Volume, age, and risk of tests currently excluded from gates." },
]} />

<Challenge>
Design a quarantine registry schema and policy. Add one scoped record, prove it still runs, simulate
expiry, show the replacement risk control, and restore it after a measurable stability threshold.
</Challenge>

<AskCommunity prompt="Test [ID] is proposed for quarantine in [scope]. Instability evidence, protected risk, replacement gate, owner/issue, expiry, and exit criterion are [values].">
These facts let reviewers judge whether temporary isolation is safer than continued blocking.
</AskCommunity>

<Resources links={[
  { href: "https://playwright.dev/docs/test-annotations", title: "Playwright Docs — tags and annotations", kind: "read" },
  { href: "https://martinfowler.com/articles/nonDeterminism.html", title: "Martin Fowler — eradicating non-determinism in tests", kind: "read" },
]} />

<Video href="https://www.youtube.com/watch?v=CnjdvotD0-I" title="Flaky Tests Foiled Forever — Craft of Testing" minutes={14} />

<Takeaways points={[
  "Quarantine changes one blocking decision; it must not erase execution or evidence.",
  "Require proven instability, risk assessment, owner, issue, scope, expiry, and exit criteria.",
  "Quarantine the narrowest failing configuration and preserve alternate gates.",
  "Measure age and volume, and fail governance when records expire.",
  "A skip without lifecycle and visibility is deletion disguised as maintenance.",
]} />

<Complete xp={10} />
