---
title: "UX heuristics"
summary: "Use established usability heuristics as inspection lenses, support findings with evidence, and avoid treating a checklist as a substitute for users."
tags: ["non-functional-testing-intro", "usability-and-accessibility", "track-c"]
updated: "2026-07-18"
related:
  - "non-functional-testing-intro/usability-and-accessibility/usability-testing"
  - "non-functional-testing-intro/usability-and-accessibility/accessibility-wcag"
  - "ui-ux-design-qa/design-principles-and-the-laws-of-ux/nielsens-10-usability-heuristics"
---

<Hook>
A control panel can expose every possible function and still make the urgent one impossible to find.
Heuristics give a tester names for that friction: visibility, match with the real world, control,
consistency, error prevention, recognition, flexibility, minimalism, recovery, and help.
</Hook>

<Callout type="analogy">
Heuristics are a mechanic's inspection lights. Each illuminates a different surface, but the light does
not decide whether the car is safe. A finding still needs a concrete task, observation, impact, and
context; [[non-functional-testing-intro/usability-and-accessibility/usability-testing]] adds real users.
</Callout>

<Term define="A usability heuristic is a broad, evidence-informed rule of thumb used to inspect an interface for likely usability problems. It is a diagnostic lens rather than a binary standard, legal requirement, or guarantee of user success.">Usability heuristic</Term>

## Turn labels into test questions

For **visibility of system status**, ask what the interface communicates after an action and whether it
arrives soon enough. For **user control and freedom**, try cancel, undo, back, and escape routes. For
**error prevention and recovery**, trigger a realistic mistake and inspect both the guardrail and the
message. For **recognition rather than recall**, check whether choices and prior context remain visible.

<Callout type="tip">
Write findings as "During task X, condition Y violates heuristic Z because the user cannot recover,
causing impact Q." The heuristic supplies shared vocabulary; the task and impact make it actionable.
</Callout>

<Callout type="mistake">
Do not award a page "9/10 heuristics passed." The heuristics overlap, are not equally important, and are
not designed as a conformance score. One irreversible data-loss path matters more than several cosmetic
consistency nits.
</Callout>

{/* IMAGE: a Japanese toilet controller with many visible buttons, icons, labels, display, and a removable cover */}
<HotspotImage
  src="/notes/non-functional-testing-intro/usability-and-accessibility/ux-heuristics.jpg"
  alt="A Japanese toilet control panel with many labeled buttons, icons, a display, and an open cover"
  credit="Wireless toilet control panel with open lid — Chris 73, Wikimedia Commons, CC BY-SA 3.0"
  creditHref="https://commons.wikimedia.org/wiki/File:Wireless_toilet_control_panel_w._open_lid.jpg"
  pins={[
    { x: 50, y: 42, label: "Status display", desc: "Visibility asks whether the system clearly confirms its current state and the result of an action." },
    { x: 13, y: 22, label: "Prominent stop control", desc: "User control asks whether a person can stop, cancel, undo, or safely leave an unwanted state." },
    { x: 53, y: 18, label: "Icons plus labels", desc: "Recognition improves when meaning is visible, but testers must confirm that symbols and language match the intended audience." },
    { x: 49, y: 76, label: "Covered advanced controls", desc: "Minimalism can hide secondary complexity while keeping frequent actions available; hiding must not remove essential access." },
  ]}
/>

<FlowAnimation
  title="A heuristic inspection that produces evidence"
  intervalMs={1600}
  nodes={[
    { label: "Choose a critical user task", desc: "Inspect a journey, not a screenshot floating outside context." },
    { label: "Apply several relevant heuristic lenses", desc: "Check feedback, language, control, prevention, recognition, consistency, and recovery." },
    { label: "Reproduce a concrete breakdown", desc: "Capture the action, state, expected signal, actual behavior, and affected user goal." },
    { label: "Prioritize by impact and validate", desc: "Use severity and user research, analytics, or support evidence; never prioritize by checklist count alone." },
  ]}
/>

<CodePlayground
  language="python"
  title="A heuristic finding oracle (Python)"
  code={`checks = {
    "task_named": True,
    "heuristic_named": True,
    "observable_breakdown": True,
    "impact_explained": True,
}
for name, passed in checks.items():
    print(name + "=" + ("PASS" if passed else "FAIL"))
result = "PASS" if all(checks.values()) else "FAIL"
assert result == "PASS", "heuristic oracle rejected vague finding"
print("RESULT=" + result)`}
/>

<CodePlayground
  language="java"
  title="A heuristic finding oracle (Java)"
  code={`import java.util.LinkedHashMap;
import java.util.Map;

public class Main {
    public static void main(String[] args) {
        Map<String, Boolean> checks = new LinkedHashMap<>();
        checks.put("task_named", true);
        checks.put("heuristic_named", true);
        checks.put("observable_breakdown", true);
        checks.put("impact_explained", true);
        boolean ok = true;
        for (var entry : checks.entrySet()) {
            System.out.println(entry.getKey() + "=" + (entry.getValue() ? "PASS" : "FAIL"));
            ok &= entry.getValue();
        }
        String result = ok ? "PASS" : "FAIL";
        if (!result.equals("PASS")) throw new AssertionError("heuristic oracle rejected vague finding");
        System.out.println("RESULT=" + result);
    }
}`}
/>

<FirstTime title="Inspect one risky journey with four lenses">
<StepChecklist steps={[
  { text: "Name the journey and intended user", detail: "Choose a consequential path such as deleting, paying, publishing, or changing permissions." },
  { text: "Probe feedback, control, prevention, and recovery", detail: "For every action, ask what the person sees, how they escape, what prevents a mistake, and how they recover." },
  { text: "Capture one concrete breakdown", detail: "Record the exact state, action, observed result, expected signal, and user impact." },
  { text: "Validate priority", detail: "Compare with participant behavior, analytics, support evidence, and product risk rather than counting heuristic labels." },
]}/>
</FirstTime>

<WhenItBreaks items={[
  { symptom: "Reviewers produce dozens of subjective comments.", fix: "Anchor each comment to a task, observable breakdown, named heuristic, and impact. Remove preference-only claims." },
  { symptom: "Two heuristics appear to describe the same defect.", fix: "Choose the label that best explains the mechanism, note a secondary lens only if useful, and file one finding rather than duplicates." },
  { symptom: "The checklist is green but users still fail.", fix: "Run representative usability sessions. Heuristics predict likely friction; they do not replace observed human behavior." },
]}/>

<WhereToCheck>
- Loading, saving, and background operations for visible system status.
- Destructive flows for cancel, undo, confirmation, and recovery.
- Labels, icons, ordering, and platform conventions for consistency and real-world match.
- Error messages for plain language, preserved input, and a next action.
</WhereToCheck>

<WorkedExample title="delete with no way back">
1. A tester deletes a saved report and receives only a brief "Done" toast.
2. The task is consequential, there is no confirmation, and no undo or recovery path is visible.
3. The finding uses error prevention plus user control: one mistaken click permanently removes work.
4. The team adds a specific confirmation and a timed undo, then tests keyboard and screen-reader paths.
</WorkedExample>

<Quiz
  question="What makes a heuristic finding actionable?"
  options={[
    "A numeric score for all ten heuristics",
    "A concrete task, observable breakdown, relevant heuristic, and user impact",
    "The reviewer's visual preference",
    "Using as many heuristic labels as possible"
  ]}
  answer={1}
  explain="A heuristic is useful vocabulary, but the reproducible task and user impact are what let a team understand and prioritize the defect."
/>

<Flashcards cards={[
  { front: "Heuristic", back: "A diagnostic rule of thumb, not a binary conformance standard." },
  { front: "Visibility", back: "The system communicates current state and action results in time to guide the user." },
  { front: "Control and recovery", back: "People can cancel, undo, escape, and recover from realistic mistakes." },
]}/>

<Challenge>
Inspect a destructive action with visibility, control, prevention, and recovery lenses. File only the
highest-impact evidence-based issue, and explain why it outranks cosmetic inconsistencies.
</Challenge>

<AskCommunity prompt="Which usability heuristic most often reveals a high-impact defect in your product, and what concrete task exposed it?" />

<Resources links={[
  { href: "https://www.nngroup.com/articles/ten-usability-heuristics/", title: "Nielsen Norman Group — 10 Usability Heuristics", kind: "read" },
  { href: "https://www.w3.org/WAI/test-evaluate/", title: "W3C WAI — Evaluating Web Accessibility Overview", kind: "read" },
  { href: "https://www.youtube.com/watch?v=6_e1AiOZHC0", title: "Nehmat Gereige — 📊 10 Usability Heuristics Explained with Real UX Examples (Nielsen Norman Group)", kind: "watch" },
]}/>

<Video href="https://www.youtube.com/watch?v=6_e1AiOZHC0" title="📊 10 Usability Heuristics Explained with Real UX Examples (Nielsen Norman Group)" minutes={13} />

<Takeaways points={[
  "Heuristics are inspection lenses, not scores or guarantees.",
  "Start with a user task and turn each label into a test question.",
  "A strong finding combines a reproducible breakdown, heuristic mechanism, and user impact.",
  "Prioritize by severity and validate with user or product evidence, not checklist totals.",
]}/>

<Complete xp={10} />
