---
title: "Constraints and Referential Integrity"
summary: "Design and verify PostgreSQL constraints as executable invariants, including safe validation on large tables."
tags: ["postgresql", "constraints", "foreign-keys", "data-integrity"]
updated: "2026-07-17"
related: ["relational-databases-engineer-level/data-integrity-at-scale/finding-orphans-and-duplicates", "relational-databases-engineer-level/data-integrity-at-scale/migrations-and-etl-verification", "relational-databases-engineer-level/programmable-objects/error-handling-in-sql"]
---

<Hook>Application validation is a polite request; a database constraint is a boundary every writer must cross. At scale, correctness includes both the invariant and a rollout that does not freeze the system.</Hook>

<Callout type="analogy">Constraints are a perimeter fence. Primary and unique keys control identity, checks define allowed shapes, and foreign keys keep children inside a valid parent boundary. A gate nobody inspects is only decoration.</Callout>

## Make invariants executable

Use `NOT NULL`, `CHECK`, `UNIQUE`, `PRIMARY KEY`, and <Term define="A constraint requiring each applicable child key to match an eligible parent key.">`FOREIGN KEY`</Term> for facts the database must defend. PostgreSQL does not automatically create an index on the referencing side of a foreign key, so parent updates or deletes may need an explicit child-column index.

<HotspotImage src="/notes/relational-databases-engineer-level/data-integrity-at-scale/constraints-and-referential-integrity.jpg" alt="A chain-link fence representing database integrity boundaries" credit="Photo: Evan-Amos — Wikimedia Commons, CC0" creditHref="https://commons.wikimedia.org/wiki/File:Chain-link-Fence.jpg" pins={[
  { x: 23, y: 50, label: "Identity", desc: "Primary and unique constraints prevent two rows from claiming the same governed key." },
  { x: 51, y: 36, label: "Allowed shape", desc: "NOT NULL and CHECK reject incomplete or invalid row states." },
  { x: 78, y: 54, label: "Relationship", desc: "A foreign key requires each non-null child key to match an eligible parent key." },
]}/>

<Callout type="tip">For large existing tables, add a CHECK or FOREIGN KEY as `NOT VALID`, repair historical rows, then run `VALIDATE CONSTRAINT`. New and updated rows are still checked while validation is pending.</Callout>

<Callout type="mistake">Assuming a foreign key creates the child-side index. It does not; inspect the query workload and index the referencing columns when parent changes or joins require it.</Callout>

<FlowAnimation title="Safe constraint rollout" intervalMs={1300} nodes={[
  { label: "Define invariant", desc: "Name the precise row or relationship rule and its null semantics." },
  { label: "Detect violations", desc: "Measure historical bad rows before changing production behavior." },
  { label: "Enforce forward", desc: "Add a supported CHECK or FOREIGN KEY as NOT VALID so new writes comply." },
  { label: "Repair + validate", desc: "Backfill violations, validate the named constraint, and monitor lock impact." },
]}/>

<CodePlayground language="python" title="Model relationship validation" code={`customers = {10, 20}
orders = [
    {"id": 1, "customer_id": 10, "total": 8},
    {"id": 2, "customer_id": 99, "total": 4},
    {"id": 3, "customer_id": 20, "total": -1},
]

violations = []
for order in orders:
    if order["customer_id"] not in customers:
        violations.append((order["id"], "foreign key"))
    if order["total"] < 0:
        violations.append((order["id"], "check total >= 0"))
print(violations)
assert len(violations) == 2`}/>

<CodePlayground language="java" title="Validate parent references in Java" code={`import java.util.*;
class Main {
  public static void main(String[] args) {
    Set<Integer> customers = Set.of(10, 20);
    Map<Integer,Integer> orders = Map.of(1, 10, 2, 99, 3, 20);
    List<Integer> orphans = new ArrayList<>();
    orders.forEach((id, customer) -> { if (!customers.contains(customer)) orphans.add(id); });
    if (!orphans.equals(List.of(2))) throw new AssertionError(orphans);
    System.out.println("orphan order ids: " + orphans);
  }
}`}/>

<FirstTime title="Audit one relationship"><StepChecklist steps={[
  { text: "State the invariant", detail: "Define identity, null behavior, and delete/update policy in plain language." },
  { text: "Query current violations", detail: "Count and sample invalid rows before adding enforcement." },
  { text: "Inspect indexes", detail: "Verify parent uniqueness and decide whether child columns need an index." },
  { text: "Roll out and validate", detail: "Enforce future writes, repair history, then confirm validation state." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "Adding the constraint blocks a busy table.", fix: "Rehearse lock behavior; use NOT VALID plus later VALIDATE where PostgreSQL supports it." },
  { symptom: "Parent deletes become unexpectedly slow.", fix: "Inspect an index beginning with the referencing foreign-key columns and verify with EXPLAIN." },
  { symptom: "A CHECK allows null unexpectedly.", fix: "CHECK succeeds for true or null; add NOT NULL when absence itself is forbidden." },
]}/>

<WhereToCheck>Inspect `pg_constraint`, `information_schema.table_constraints`, `referential_constraints`, `pg_indexes`, constraint validation state, SQLSTATE, and lock waits during rehearsal.</WhereToCheck>

<WorkedExample title="Adding an order-customer foreign key">Count orders whose non-null customer_id has no customer. Add `orders_customer_fk` as NOT VALID, verify a new orphan is rejected, repair historical orphans, then `VALIDATE CONSTRAINT`. Confirm `convalidated` and benchmark parent update/delete with the child index.</WorkedExample>

<Quiz question="What does a PostgreSQL NOT VALID foreign key allow?" options={["New invalid rows", "Skipping the initial historical scan while checking new or updated rows", "Disabling the constraint permanently", "Dropping parent uniqueness"]} answer={1} explain="It separates forward enforcement from validation of preexisting data." />

<Flashcards cards={[
  { front: "Referential integrity", back: "Every applicable child key matches an eligible parent key under the declared policy." },
  { front: "NOT VALID", back: "Adds supported constraints without first scanning all historical rows; new writes are enforced." },
  { front: "CHECK and null", back: "A CHECK rejects false, but null passes; pair with NOT NULL when required." },
]}/>

<Challenge>Pick one high-volume relationship. Produce the violation query, null policy, action policy, index check, NOT VALID rollout, repair rule, and VALIDATE gate.</Challenge>

<AskCommunity prompt="Invariant: [rule]. PostgreSQL version: [version]. Current violations: [count]. Proposed constraint/index: [DDL]. Rehearsed locks/timing: [evidence]. Is rollout safe?">Include table size and write rate; safe DDL is workload-specific.</AskCommunity>

<Resources links={[
  { href: "https://www.postgresql.org/docs/current/ddl-constraints.html", title: "PostgreSQL — Constraints", kind: "read" },
  { href: "https://www.postgresql.org/docs/current/sql-altertable.html", title: "PostgreSQL — ALTER TABLE", kind: "read" },
]}/>

<Video href="https://www.youtube.com/watch?v=qw--VYLpxG4" title="Learn PostgreSQL — Full Course for Beginners" minutes={260} />

<Takeaways points={["Constraints defend invariants against every writer.", "CHECK does not imply NOT NULL, and foreign keys do not create child-side indexes.", "NOT VALID can separate forward enforcement from historical validation.", "Measure violations and rehearse locks before production DDL."]}/>

<Complete xp={10} />
