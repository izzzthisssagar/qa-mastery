---
title: "ER modeling from requirements"
summary: "Turn requirement nouns, business events, cardinalities, optionality, and lifecycle rules into a conceptual entity-relationship model before choosing tables and datatypes."
tags: ["relational-databases-engineer-level", "schema-design", "track-e"]
updated: "2026-07-17"
related:
  - "relational-databases-engineer-level/schema-design/keys-and-relationships"
  - "relational-databases-engineer-level/schema-design/normalization-1nf-to-3nf"
  - "relational-databases-engineer-level/sql-mastery/date-time-and-timezone-handling"
---

<Hook>If the requirement says “a user can belong to many teams” and your diagram has one `team_id` on users, the schema is already arguing with the product before either exists.</Hook>
<Callout type="analogy">An architectural drawing turns activities and constraints into rooms and connections before anyone pours concrete. ER modeling does that for information.</Callout>
<Term define="Entity-relationship (ER) modeling is conceptual design that identifies entity types, their attributes, relationships, cardinalities, optionality, and business rules before mapping them to relational tables and constraints.">ER modeling</Term>

## Mine rules, not just nouns

For “projects contain tickets; users may watch many tickets; every ticket has exactly one project,”
model `Project`, `Ticket`, `User`, and a many-to-many `Watch` relationship. Ask lifecycle questions:
can a ticket exist before its project, can a user watch twice, and what happens when either is deleted?

<Callout type="tip">Write every relationship in both directions: “one project has zero-or-many tickets; each ticket belongs to exactly one project.” Ambiguity hates symmetry.</Callout>
<Callout type="mistake">Treating every noun as an entity. “Status” may be an attribute, constrained value, or entity depending on behavior and ownership.</Callout>

<HotspotImage src="/notes/relational-databases-engineer-level/schema-design/er-modeling-from-requirements.jpg" alt="Architectural perspective drawing of a public building" credit="Public building architectural drawing — Cusack5239, Wikimedia Commons, CC BY-SA 3.0" creditHref="https://commons.wikimedia.org/wiki/File:Public_building.jpg" pins={[
  { x: 25, y: 58, label: "Entities", desc: "Stable things the requirements need to identify." },
  { x: 53, y: 40, label: "Relationships", desc: "Business connections and events between entities." },
  { x: 78, y: 61, label: "Constraints", desc: "Cardinality, optionality, uniqueness, and lifecycle rules." },
]}/>

<FlowAnimation title="Requirements to conceptual model" intervalMs={1500} nodes={[
  { label: "Collect scenarios", desc: "Happy paths, refusal paths, lifecycle, and history." },
  { label: "Identify entities", desc: "Things with identity and independent meaning." },
  { label: "Name relationships", desc: "Use verbs and describe both directions." },
  { label: "Set cardinality", desc: "One, optional one, many, and mandatory many." },
  { label: "Validate examples", desc: "Walk real and adversarial scenarios through the model." },
]}/>

<CodePlayground language="python" title="Run it — validate requirement cardinalities (Python)" code={`requirements = {"ticket_project": (1, 1), "project_tickets": (0, None), "ticket_watchers": (0, None)}
examples = {"ticket_project": 1, "project_tickets": 3, "ticket_watchers": 0}
for name, (minimum, maximum) in requirements.items():
    count = examples[name]
    valid = count >= minimum and (maximum is None or count <= maximum)
    print(f"{name}: {count} -> {'VALID' if valid else 'INVALID'}")

# ticket_project: 1 -> VALID
# project_tickets: 3 -> VALID
# ticket_watchers: 0 -> VALID`}/>

<CodePlayground language="java" title="Run it — validate requirement cardinalities (Java)" code={`import java.util.*;
public class Main {
  record Rule(int min,Integer max){}
  public static void main(String[] args){
    Map<String,Rule> rules=new LinkedHashMap<>(); rules.put("ticket_project",new Rule(1,1)); rules.put("project_tickets",new Rule(0,null)); rules.put("ticket_watchers",new Rule(0,null));
    Map<String,Integer> counts=Map.of("ticket_project",1,"project_tickets",3,"ticket_watchers",0);
    rules.forEach((n,r)->{int c=counts.get(n); boolean ok=c>=r.min()&&(r.max()==null||c<=r.max()); System.out.println(n+": "+c+" -> "+(ok?"VALID":"INVALID"));});
  }
}

/* ticket_project: 1 -> VALID
   project_tickets: 3 -> VALID
   ticket_watchers: 0 -> VALID */`}/>

<FirstTime title="Your mission: model one workflow"><StepChecklist steps={[
  { text: "Collect five concrete scenarios", detail: "Include creation, reassignment, deletion, duplicates, and history." },
  { text: "Underline candidate entities and relationship verbs", detail: "Reject nouns with no independent identity or behavior." },
  { text: "State cardinality and optionality both ways", detail: "Use exact minimum and maximum language." },
  { text: "Walk counterexamples through the diagram", detail: "Ask what invalid states the model must prevent." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "Stakeholders disagree on cardinality.", fix: "Use concrete examples and lifecycle events; the disagreement is a requirement gap, not a diagram formatting issue." },
  { symptom: "A relationship needs its own attributes.", fix: "Promote it to an associative entity, such as Watch with created_at and notification_mode." },
  { symptom: "The model cannot represent history.", fix: "Separate current state from temporal events or validity periods according to reporting requirements." },
]}/>

<WhereToCheck>
- User stories, API contracts, event payloads, and audit requirements.
- Cardinality in both directions and delete/reassignment behavior.
- Examples of invalid states the future schema must reject.
</WhereToCheck>
<WorkedExample title="watchers reveal the missing entity">A simple user-ticket line models many-to-many membership. Requirements later demand `watched_at` and notification mode. The relationship becomes a `Watch` entity keyed by user and ticket, with its own attributes and uniqueness rule.</WorkedExample>
<Quiz question="When should a relationship become an associative entity?" options={["Whenever the diagram is large", "When the relationship has attributes or identity of its own", "Only for one-to-one", "Never"]} answer={1} explain="A relationship carrying facts such as timestamps or roles needs a place to store and constrain them." />
<Flashcards cards={[
  { front: "Entity", back: "A distinguishable thing with independent meaning and identity." },
  { front: "Cardinality", back: "Minimum and maximum relationship participation." },
  { front: "Optionality", back: "Whether zero related instances are allowed." },
  { front: "Associative entity", back: "A modeled relationship with its own attributes or identity." },
]}/>
<Challenge>Model projects, tickets, users, assignees, and watchers from ten written rules, including deletion and duplicate behavior.</Challenge>
<AskCommunity prompt="My ER model assumes `[cardinality]` for `[relationship]`; these two scenarios conflict.">Ask for the missing business rule, not opinions about crow's-foot notation.</AskCommunity>
<Resources links={[
  { href: "https://www.postgresql.org/docs/current/ddl-basics.html", title: "PostgreSQL — table basics", kind: "read" },
  { href: "https://www.postgresql.org/docs/current/ddl-constraints.html", title: "PostgreSQL — constraints", kind: "read" },
]}/>
<Video href="https://www.youtube.com/watch?v=ztHopE5Wnpc" title="freeCodeCamp — database design course" minutes={487} />
<Takeaways points={[
  "Start with scenarios and business rules, not column types.",
  "Describe every relationship in both directions with minimum and maximum cardinality.",
  "Associative entities model many-to-many facts with their own attributes.",
  "Counterexamples expose missing lifecycle and integrity rules before implementation.",
]}/>
<Complete xp={10} />
