---
title: "Full API suite on BuggyAPI"
summary: "Assemble a risk-based, isolated suite across BuggyAPI health, authentication, projects, tickets, contracts, and cleanup—without encoding hidden bug answers."
tags: ["api-test-automation", "real-world-api-suites", "track-d"]
updated: "2026-07-17"
related:
  - "api-test-automation/real-world-api-suites/test-pyramids-for-apis"
  - "api-test-automation/real-world-api-suites/data-setup-via-api"
  - "api-test-automation/real-world-api-suites/chaining-and-state"
---

<Hook>A “full suite” is not every endpoint hit once. It is a deliberate system of contracts, lifecycles, negative boundaries, isolation, and evidence that still tells the truth on run fifty.</Hook>

<Callout type="analogy">Mission control does not watch one light and declare the spacecraft healthy. Separate consoles watch separate risks, then combine signals into one operational picture.</Callout>

<Term define="A full BuggyAPI suite is a layered, sandbox-scoped collection of independent scenarios covering public health and OpenAPI surfaces, authentication, project and ticket lifecycles, filtering and pagination, negative permissions and validation, plus reliable cleanup. It tests documented behavior without embedding private seeded-bug manifests or answers.">Full BuggyAPI suite</Term>

## Build from public behavior and risk

Start with `/api/health`, `/api/v1/openapi.json`, and the interactive docs at `/api/docs`. Authenticated REST routes include `/api/v1/me`, projects, tickets, and ticket attachments; the host also exposes GraphQL and SOAP practice surfaces. Use only credentials provisioned for your own sandbox. Derive assertions from the public contract and observed requirements, never from internal seeded-bug files.

<Callout type="tip">Tag scenarios `smoke`, `contract`, `lifecycle`, `negative`, and `regression`. A suite becomes operable when CI can select risk, not just “run everything.”</Callout>

<Callout type="mistake">Do not assert one enormous response snapshot. It couples the suite to irrelevant values and hides which contract actually broke.</Callout>

<HotspotImage src="/notes/api-test-automation/real-world-api-suites/full-api-suite-on-buggyapi.jpg" alt="NASA mission control room during a Space Shuttle simulation with rows of consoles and large status screens" credit="Mission Control Center during STS-114 simulation — NASA, public domain, via Wikimedia Commons" creditHref="https://commons.wikimedia.org/wiki/File:Mission_control_center.jpg" pins={[
  { x: 22, y: 68, label: "Auth and readiness console", desc: "Fast smoke proves the target and credentials before deeper scenarios begin." },
  { x: 52, y: 62, label: "Lifecycle consoles", desc: "Projects and tickets are independent domains with owned setup and cleanup." },
  { x: 76, y: 22, label: "Shared status screens", desc: "Reports combine domain signals while keeping each failing assertion identifiable." },
]}/>

<FlowAnimation title="BuggyAPI suite architecture" intervalMs={1500} nodes={[
  { label: "Health + public contract", desc: "Confirm target identity and load the OpenAPI source." },
  { label: "Authenticate own sandbox", desc: "Verify principal through /api/v1/me." },
  { label: "Independent domain scenarios", desc: "Projects, tickets, filters, pagination, and negatives arrange their own data." },
  { label: "Contract + behavior assertions", desc: "Check focused status, headers, shape, and business invariants." },
  { label: "Scoped cleanup + report", desc: "Delete owned resources and retain actionable evidence." },
]}/>

<CodePlayground language="python" title="Run it - build a risk-based BuggyAPI execution plan (Python)" code={`suites = {"smoke": 4, "contract": 7, "lifecycle": 9, "negative": 6}
total = 0
for name, count in suites.items():
    total += count
    print(f"{name}: {count} scenarios")
print(f"total={total}")
print("order=smoke -> contract -> lifecycle -> negative -> cleanup")`}/>

<CodePlayground language="java" title="Run it - build the same execution plan (Java)" code={`import java.util.*;
public class Main {
  public static void main(String[] args) {
    Map<String,Integer> suites = new LinkedHashMap<>();
    suites.put("smoke",4); suites.put("contract",7); suites.put("lifecycle",9); suites.put("negative",6);
    int total = 0;
    for (var entry : suites.entrySet()) { total += entry.getValue(); System.out.println(entry.getKey() + ": " + entry.getValue() + " scenarios"); }
    System.out.println("total=" + total);
    System.out.println("order=smoke -> contract -> lifecycle -> negative -> cleanup");
  }
}`}/>

<FirstTime title="Your mission: assemble the capstone suite"><StepChecklist steps={[
  { text: "Prove /api/health and load /api/v1/openapi.json", detail: "Use the public contract as the source, not internal code comments." },
  { text: "Authenticate and verify /api/v1/me", detail: "Use only your provisioned sandbox credentials." },
  { text: "Cover isolated project and ticket lifecycles", detail: "Each scenario captures ids and owns cleanup." },
  { text: "Add focused negative and contract checks", detail: "Assert documented invariants without revealing seeded bug answers." },
  { text: "Run headlessly with reports", detail: "Make smoke selectable for PRs and broader coverage schedulable." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "Every scenario fails with 401.", fix: "Stop after auth smoke; inspect header scheme, credential source, expiry, and sandbox identity before running domain tests." },
  { symptom: "Runs interfere with another learner.", fix: "Verify every request uses your sandbox credentials and every created name has your unique run id." },
  { symptom: "One create failure causes twenty red requests.", fix: "Gate dependent steps on validated setup and report the first causal failure; skip unsafe consumers." },
  { symptom: "Suite passes while cleanup fails.", fix: "Report cleanup as a distinct failure because leaked state will corrupt future runs." },
]}/>

<WhereToCheck>
- `/api/health`, `/api/docs`, and `/api/v1/openapi.json` for public runtime truth.
- The authenticated principal from `/api/v1/me` and sandbox-scoped credentials.
- Run-id names, captured ids, first causal failure, and cleanup ledger.
- CLI/JUnit/JSON results grouped by smoke, contract, lifecycle, and negative tags.
</WhereToCheck>

<WorkedExample title="one full run that remains diagnosable">
Smoke verifies health, OpenAPI availability, auth, and `/me`. Contract checks validate focused response shapes. Project and ticket scenarios each create uniquely named data, capture server ids, test read/update/delete behavior and documented filters, then clean their own records. Negative cases use fresh setup and explicit invalid inputs. Reports identify the domain and first failed invariant—never a hidden bug label.
</WorkedExample>

<Quiz question="What is the correct source for BuggyAPI assertions in this learner suite?" options={["Private seeded-bug files", "The public OpenAPI/docs and stated behavior", "Another learner's captured responses", "Whatever makes the test pass"]} answer={1} explain="The exercise is to discover and test public behavior. Encoding hidden manifests leaks answers and makes the suite dishonest."/>

<Flashcards cards={[
  { front: "BuggyAPI public starting points", back: "/api/health, /api/docs, and /api/v1/openapi.json." },
  { front: "Core suite domains", back: "Auth identity, projects, tickets, filtering/pagination, contracts, negatives, and cleanup." },
  { front: "Capstone isolation rule", back: "Use only your sandbox and delete only ids created by your run." },
  { front: "What never belongs in the notes?", back: "Private seeded-bug manifests, labels, or answer-revealing assertions." },
]}/>

<Challenge>Create a collection plan with at least four smoke, six contract, eight lifecycle, and six negative scenarios. For each, state its public requirement, setup ownership, first assertion, and cleanup rule—without naming any seeded defect.</Challenge>

<AskCommunity prompt="My BuggyAPI suite first fails at `[public route/assertion]`; run tag, redacted response shape, and owned setup ids are `[details]`.">Do not share sandbox keys, passwords, tokens, internal manifests, or discovered answer lists.</AskCommunity>

<Resources links={[
  { href: "https://learning.postman.com/docs/tests-and-scripts/write-scripts/test-examples/", title: "Postman Docs — Test script examples", kind: "read" },
  { href: "https://learning.postman.com/docs/collections/running-collections/running-collections-overview", title: "Postman Docs — Run and test collections", kind: "reference" },
]}/>

<Video href="https://www.youtube.com/watch?v=rxEwPMM_Qyc" title="API Challenges: a practice API application for testing tutorials — EvilTester" minutes={8} />

<Takeaways points={[
  "Build the suite from public contracts and risk, never hidden seeded-bug answers.",
  "Separate smoke, contract, lifecycle, negative, and regression coverage so CI can select intent.",
  "Use only your sandbox, unique run data, captured ids, and scoped cleanup.",
  "Stop cascades at failed setup and report the first causal invariant with actionable evidence.",
  "A full suite is operable, repeatable, and diagnosable—not merely large.",
]}/>

<Complete xp={10} />
