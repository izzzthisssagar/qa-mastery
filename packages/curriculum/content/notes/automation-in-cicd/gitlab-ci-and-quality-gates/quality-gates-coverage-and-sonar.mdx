---
title: "Quality gates (coverage, Sonar)"
summary: "A quality gate converts a small set of measurable risk policies into a pass/fail decision; coverage and static analysis are useful signals only when scoped, explainable, and resistant to gaming."
tags: ["quality-gates", "coverage", "sonarqube", "track-d"]
updated: "2026-07-17"
related:
  - "automation-in-cicd/gitlab-ci-and-quality-gates/blocking-a-merge-on-failure"
  - "automation-in-cicd/gitlab-ci-and-quality-gates/gitlab-ci-yml"
  - "automation-in-cicd/scheduling-and-reporting/dashboards"
---

<Hook>
Coverage can rise while confidence falls. A test that executes every line without asserting the
outcome may produce a beautiful percentage and prove nothing. A gate is valuable when it blocks a
specific unacceptable risk, not when it turns a convenient number into a superstition.
</Hook>

<Callout type="analogy">
A caliper is precise, but it measures only the dimension between its jaws. It cannot tell whether the
part is strong, correctly assembled, or safe. Coverage and static analysis are the same: exact within
their definitions, incomplete as a verdict on overall quality.
</Callout>

<Term define="A quality gate is an automated policy that evaluates selected measurable conditions and returns pass or fail for a change or codebase. Examples include no new blocker findings, coverage on changed code above a threshold, successful tests, or bounded duplication. SonarQube/SonarCloud can compute a Quality Gate from analysis; GitLab can ingest coverage and code-quality reports. A gate should focus on new-code risk, declare ownership and exceptions, and remain only one part of the release decision.">quality gate</Term>

## Gate behavior, not vanity

Good policies are small and actionable:

- all required automated tests pass;
- no new critical/blocker security or reliability findings;
- changed-code coverage does not fall below an agreed risk threshold;
- analysis completed for the exact revision;
- any exception has an owner, reason, expiry, and review.

Repository-wide coverage targets punish legacy code unrelated to a small change and encourage shallow
tests. New-code policies create a ratchet without pretending the existing system became safe overnight.

<Callout type="tip">
Publish the report and a short failure summary. A red gate that says only "quality failed" creates
work; one that links new findings, rules, files, and remediation makes work finish.
</Callout>

<Callout type="mistake">
Failing on every analyzer warning from day one. Teams drown in old findings, mark the job allowed to
fail, and teach everyone that the gate is ceremonial.
</Callout>

<HotspotImage src="/notes/automation-in-cicd/gitlab-ci-and-quality-gates/quality-gates-coverage-and-sonar.jpg" alt="A metal vernier caliper measuring a small black circular object against a millimetre scale" credit="Caliper full view — Simon A. Eugster, CC BY-SA 3.0" creditHref="https://commons.wikimedia.org/wiki/File:Caliper_full_view.jpeg" pins={[
  { x: 17, y: 60, label: "Measured scope", desc: "A metric covers a defined property, not the entire product." },
  { x: 34, y: 43, label: "Threshold", desc: "The gate compares an observed value or severity with an agreed boundary." },
  { x: 60, y: 39, label: "Calibration", desc: "Rules, exclusions, versions, and baselines must be governed so results remain comparable." },
  { x: 88, y: 42, label: "Limit of claim", desc: "Precision does not expand scope: unmeasured risks still require tests and review." },
]} />

<FlowAnimation title="From analysis to merge signal" intervalMs={1500} nodes={[
  { label: "Exact revision checked out", desc: "Analysis and tests bind to the candidate commit." },
  { label: "Evidence generated", desc: "Tests, coverage, and analyzers produce machine-readable reports." },
  { label: "Reports parsed", desc: "GitLab or Sonar maps findings to changed code and configured rules." },
  { label: "Policy evaluated", desc: "New-code thresholds and prohibited severities produce pass/fail." },
  { label: "Evidence displayed", desc: "Merge-request annotations explain what crossed the boundary." },
  { label: "Exception governed", desc: "Any override is rare, owned, reasoned, time-bounded, and auditable." },
]} />

<CodePlayground language="python" title="Run it — evaluate a new-code gate (Python)" code={```evidence = {"tests_pass": True, "new_coverage": 84, "new_blockers": 0}
policy = {"minimum_coverage": 80, "maximum_blockers": 0}
checks = {
    "tests": evidence["tests_pass"],
    "coverage": evidence["new_coverage"] >= policy["minimum_coverage"],
    "blockers": evidence["new_blockers"] <= policy["maximum_blockers"],
}
print(checks)
print("gate:", "PASS" if all(checks.values()) else "FAIL")```} />

<CodePlayground language="java" title="Run it — evaluate a new-code gate (Java)" code={```import java.util.*;

public class Main {
    public static void main(String[] args) {
        boolean testsPass = true;
        int newCoverage = 84, newBlockers = 0;
        var checks = new LinkedHashMap<String, Boolean>();
        checks.put("tests", testsPass);
        checks.put("coverage", newCoverage >= 80);
        checks.put("blockers", newBlockers <= 0);
        System.out.println(checks);
        System.out.println("gate: " + (checks.values().stream().allMatch(Boolean::booleanValue) ? "PASS" : "FAIL"));
    }
}```} />

<FirstTime title="Your mission: design one defensible gate">
<StepChecklist steps={[
  { text: "Name the risk and evidence source", detail: "For example: untested new branches, measured by changed-code branch coverage." },
  { text: "Choose a threshold from history and consequence", detail: "Do not copy an industry number without examining current variance and criticality." },
  { text: "Make one change fail on purpose", detail: "Confirm report, annotation, status, and remediation path are understandable." },
  { text: "Document exception governance", detail: "Specify authorized role, required reason, owner, expiry, and follow-up." },
]} />
A gate is ready when failure teaches the author exactly what policy was violated and why it matters.
</FirstTime>

<WhenItBreaks items={[
  { symptom: "Coverage is missing or zero in GitLab.", fix: "Separate log regex percentage from coverage_report format/path; verify the tool generated the expected Cobertura or JaCoCo file." },
  { symptom: "Sonar reports analysis but the pipeline passes a failed gate.", fix: "Ensure the scanner waits for and propagates Quality Gate status with a bounded timeout." },
  { symptom: "Developers add assertions-free tests to raise coverage.", fix: "Review test strength, mutation results or defect detection, and focus policy on risk—not percentage alone." },
  { symptom: "Old findings block every change.", fix: "Baseline legacy debt and gate new code while separately owning debt reduction." },
]} />

<WhereToCheck>
- **Exact analyzed SHA and branch/MR context** — evidence must match the candidate.
- **Raw report path/format** — generation versus ingestion failure.
- **Quality profile/gate version** — rule or threshold drift.
- **New-code definition and exclusions** — actual scope of the metric.
- **Merge-request annotations and analyzer task log** — actionable failure reason.
</WhereToCheck>

<WorkedExample title="80% coverage that missed the refund bug">
1. A service has 86% line coverage and a passing global threshold.
2. Refund tests execute the handler but never assert the amount sent to the payment provider.
3. A sign error ships despite green coverage.
4. The team adds outcome assertions and reviews changed-code branch coverage for money paths.
5. Coverage remains a useful gap detector, but risk-focused assertions become the actual confidence.
</WorkedExample>

<Quiz question="What does 90% line coverage prove?" options={[
  "The product is 90% correct",
  "Tests executed 90% of instrumented lines under that measurement; assertion strength and other risks remain unknown",
  "No security defects exist",
  "Every branch and input was tested"
]} answer={1} explain="Coverage measures execution, not correctness, assertion quality, branch completeness, realistic data, or operational risk." />

<Flashcards cards={[
  { front: "Quality gate", back: "Automated pass/fail policy over selected evidence, not a total quality verdict." },
  { front: "New-code gate", back: "Applies policy to changed code, creating a ratchet without blocking all work on legacy debt." },
  { front: "Line coverage claim", back: "Which instrumented lines executed; it says nothing direct about assertion strength." },
  { front: "Actionable gate failure", back: "Names policy, evidence, changed location, remediation, and report link." },
  { front: "Governed override", back: "Authorized, reasoned, owned, time-bounded, auditable, and followed up." },
]} />

<Challenge>
Take one current gate and write its exact claim, blind spots, baseline, threshold rationale, owner,
failure remediation, false-positive rate, and override history. Replace one vanity rule with a risk rule.
</Challenge>

<AskCommunity prompt="Gate [name/version] failed on SHA [sha] because [policy]. Raw evidence is [value/report], new-code scope is [scope], and expected remediation is [action].">
Share evidence and configuration, not confidential source or tokens.
</AskCommunity>

<Resources links={[
  { href: "https://docs.gitlab.com/ci/yaml/artifacts_reports/", title: "GitLab Docs — CI/CD artifact report types", kind: "read" },
  { href: "https://docs.sonarsource.com/sonarqube-server/latest/user-guide/quality-gates/", title: "SonarQube Docs — Quality gates", kind: "read" },
]} />

<Video href="https://www.youtube.com/watch?v=8_Xt9vchlpY" title="How to Create Quality Gates in SonarQube — Engineerhoon" minutes={7} />

<Takeaways points={[
  "A quality gate blocks a defined unacceptable risk; it does not certify the entire product.",
  "Coverage measures executed code, not assertion strength or correctness.",
  "Prefer actionable new-code policies over overwhelming legacy-wide thresholds.",
  "Bind analysis to the exact revision and publish understandable report evidence.",
  "Keep overrides rare, authorized, owned, time-bounded, and auditable.",
]} />

<Complete xp={10} />
