---
title: "Testing Procedures"
summary: "Turn procedure testing into a repeatable arrange-call-assert-rollback discipline with exact state diffs."
tags: ["postgresql", "procedures", "integration-testing", "transactions"]
updated: "2026-07-17"
related: ["relational-databases-engineer-level/programmable-objects/stored-procedures-and-functions", "relational-databases-engineer-level/programmable-objects/triggers", "relational-databases-engineer-level/data-integrity-at-scale/migrations-and-etl-verification"]
---

<Hook>A procedure test without a state diff is a smoke signal, not proof. Passing means the intended rows changed, forbidden rows did not, invariants held, and failures left no debris.</Hook>

<Callout type="analogy">Testing a procedure is a bridge <Term define="A controlled test that applies known demand and measures the resulting behavior.">load test</Term>. The bridge must carry the designed load, refuse unsafe conditions, return to a known state, and leave evidence engineers can compare.</Callout>

## Build a deterministic harness

Use a dedicated schema or transaction-isolated fixture, stable identifiers, and assertions that identify rows by key. Prefer `BEGIN`/`ROLLBACK` around tests when the procedure does not control transactions itself. If it does, provision disposable data and clean it by owned keys.

<HotspotImage src="/notes/relational-databases-engineer-level/programmable-objects/testing-procedures.jpg" alt="Engineers performing a bridge load test" credit="Umgeni River Bridge load test — Wikimedia Commons, public domain" creditHref="https://commons.wikimedia.org/wiki/File:Umgeni_River_Bridge_Load_Test.jpg" pins={[
  { x: 25, y: 59, label: "Known load", desc: "Fixtures must make input quantity and starting state explicit." },
  { x: 53, y: 42, label: "Observed structure", desc: "Measure outputs, table diffs, emitted events, and invariant checks—not only completion." },
  { x: 79, y: 60, label: "Safety margin", desc: "Boundary, overload, retry, and failure cases prove behavior beyond the demo path." },
]}/>

<Callout type="tip">Give every test run a unique key. It makes cleanup exact, exposes accidental cross-test reads, and turns logs into attributable evidence.</Callout>

<Callout type="mistake">Asserting total table counts in a shared database. Concurrent tests make counts noisy; assert the exact keys and relationships owned by this case.</Callout>

<FlowAnimation title="Procedure harness" intervalMs={1200} nodes={[
  { label: "Arrange", desc: "Insert minimal fixtures plus an unrelated sentinel." },
  { label: "Snapshot", desc: "Capture relevant rows, invariants, and expected error contract." },
  { label: "CALL", desc: "Invoke as the application role with explicit parameters." },
  { label: "Assert + clean", desc: "Diff exact rows, then rollback or remove only run-owned data." },
]}/>

<CodePlayground language="python" title="Assert an exact state diff" code={`before = {"A": 10, "B": 20, "SENTINEL": 99}
after = {"A": 7, "B": 23, "SENTINEL": 99}

changed = {key: (before[key], after[key]) for key in before if before[key] != after[key]}
assert changed == {"A": (10, 7), "B": (20, 23)}
assert sum(before.values()) == sum(after.values())
print("exact diff:", changed)
print("invariant: total preserved")`}/>

<CodePlayground language="java" title="Verify transfer invariants in Java" code={`import java.util.*;
class Main {
  public static void main(String[] args) {
    Map<String,Integer> before = Map.of("A",10,"B",20,"SENTINEL",99);
    Map<String,Integer> after = Map.of("A",7,"B",23,"SENTINEL",99);
    int beforeTotal = before.values().stream().mapToInt(Integer::intValue).sum();
    int afterTotal = after.values().stream().mapToInt(Integer::intValue).sum();
    if (beforeTotal != afterTotal || !after.get("SENTINEL").equals(99))
      throw new AssertionError("contract broken");
    System.out.println("total preserved: " + afterTotal);
  }
}`}/>

<FirstTime title="Create a procedure test"><StepChecklist steps={[
  { text: "Name the contract", detail: "List outputs, allowed row changes, invariants, errors, and retry behavior." },
  { text: "Arrange owned rows", detail: "Use stable test keys and at least one untouched sentinel." },
  { text: "Call as production does", detail: "Use the application role and matching transaction boundary." },
  { text: "Diff then clean", detail: "Assert exact rows and rollback; prove the next run starts clean." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "The test passes alone but fails in the suite.", fix: "Remove shared identifiers, wall-clock assumptions, and global counts; namespace fixtures per run." },
  { symptom: "A failed call leaves partial data.", fix: "Assert atomicity across every touched table and inspect exception handling that may swallow an error." },
  { symptom: "Cleanup deletes another test's rows.", fix: "Delete only by the unique run key or prefer rollback-based isolation." },
]}/>

<WhereToCheck>Capture procedure definition, role, transaction state, SQLSTATE, exact before/after rows, sequences, audit/event tables, and server logs. A test report should reconstruct the call.</WhereToCheck>

<WorkedExample title="Testing an account transfer">Arrange accounts A=10, B=20, sentinel=99. Call transfer(A,B,3). Assert A=7, B=23, sentinel=99, total=129, and one transfer record. Then call with 30 and assert the documented SQLSTATE plus zero row changes. Roll back and repeat to prove isolation.</WorkedExample>

<Quiz question="Why add a sentinel row to a procedure fixture?" options={["To increase row count", "To prove unrelated data remained untouched", "To avoid using transactions", "To grant permissions"]} answer={1} explain="A sentinel catches an over-broad UPDATE or DELETE that correct target assertions may miss." />

<Flashcards cards={[
  { front: "State diff", back: "The exact keyed rows and values that changed between snapshots." },
  { front: "Sentinel row", back: "Unrelated fixture data that must remain unchanged." },
  { front: "Deterministic fixture", back: "Owned, repeatable data without dependence on timing or concurrent global counts." },
]}/>

<Challenge>Write a four-case suite for one procedure: success, boundary, refusal, and retry. For each, list exact output, exact state diff, invariant, and cleanup proof.</Challenge>

<AskCommunity prompt="Procedure: [signature]. Isolation: [transaction/schema]. Input: [values]. Expected/actual keyed diff: [evidence]. SQLSTATE: [code]. What leak remains?">Include the sentinel result and whether the procedure performs transaction control.</AskCommunity>

<Resources links={[
  { href: "https://www.postgresql.org/docs/current/sql-call.html", title: "PostgreSQL — CALL", kind: "read" },
  { href: "https://www.postgresql.org/docs/current/tutorial-transactions.html", title: "PostgreSQL — Transactions", kind: "read" },
]}/>

<Video href="https://www.youtube.com/watch?v=qw--VYLpxG4" title="Learn PostgreSQL — Full Course for Beginners" minutes={260} />

<Takeaways points={["A procedure test proves exact state changes and invariants, not absence of exceptions.", "Unique run keys and sentinels make hidden coupling visible.", "Test as the application role with the production transaction boundary.", "Rollback or exact owned-key cleanup must make reruns deterministic."]}/>

<Complete xp={10} />
