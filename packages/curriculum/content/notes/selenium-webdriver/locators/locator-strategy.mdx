---
title: "A locator strategy that survives change"
summary: "Rank locator candidates by user meaning, product-owned contracts, uniqueness, and the kinds of change they should survive."
tags: ["selenium", "webdriver", "locators", "track-d"]
updated: "2026-07-18"
related:
  - "selenium-webdriver/locators/id-name-css-xpath"
  - "selenium-webdriver/locators/relative-locators"
  - "selenium-webdriver/locators/robust-selectors"
---

<Hook>
Teams often debate CSS versus XPath while ignoring the more important question: which page fact is actually promised to remain stable?
</Hook>

<Callout type="analogy">
A baggage tag carries a human-readable passenger and destination plus a machine barcode. Both locate the bag, but they serve different contracts. A product-owned test attribute is like a deliberate tracking number; a generated styling class is like ink position on this print run.
</Callout>

<Term define="A locator contract is an intentional, stable relationship between a test and an element, such as a unique product-owned ID or data attribute.">locator contract</Term>

## Make the selector prove intent

Start with meaning and ownership. A unique stable ID is concise. A product-owned `data-testid` is explicit when user-facing semantics are unavailable. Accessible roles, names, labels, and visible text can align a test with what users perceive, but text may be localized or legitimately duplicated. CSS and XPath are query languages, not durability guarantees.

Write the change budget before the selector: should it survive restyling, markup wrappers, localization, repeated cards, or reordered rows? Then inspect uniqueness in the correct browsing context. If the application lacks a stable hook, ask developers to add one rather than hiding fragility in a clever chain.

<Callout type="tip">Record why each locator was selected and what changes are allowed to break it; this turns repair into a contract discussion.</Callout>
<Callout type="mistake">Treating a generated class as stable because it is unique today. Build output can change without product behavior changing.</Callout>

<HotspotImage src="/notes/selenium-webdriver/locators/locator-strategy.jpg" alt="Lufthansa baggage tag with a passenger name, destination, barcode, and tracking number" credit="Lufthansa baggage identification tag — Matthias Sebulke, public domain" creditHref="https://commons.wikimedia.org/wiki/File:Lufthansa_baggage_identification_tag.jpg" pins={[
  { x: 25, y: 25, label: "Identity", desc: "The visible feature represents stable evidence used to identify the target." },
  { x: 70, y: 30, label: "Context", desc: "Surrounding structure narrows candidates but should not become an absolute path." },
  { x: 35, y: 70, label: "Candidate", desc: "A query can match something without proving it is the intended element." },
  { x: 75, y: 75, label: "Oracle", desc: "A decisive observed property confirms identity before interaction." },
]}/>

<FlowAnimation title="From candidate to reliable element" intervalMs={1500} nodes={[
  { label: "Name intent", desc: "State the product fact the test needs." },
  { label: "Choose evidence", desc: "Prefer owned identity or meaning over layout." },
  { label: "Measure matches", desc: "Reject absence and unintended ambiguity." },
  { label: "Verify semantics", desc: "Check a decisive property before acting." },
]}/>

## Real Selenium examples

These fenced examples require Selenium. The playgrounds are deterministic standard-library models.

```python
from selenium.webdriver.common.by import By

panel = driver.find_element(By.CSS_SELECTOR, "[data-testid='profile-panel']")
save = panel.find_element(By.CSS_SELECTOR, "button[data-action='save']")
assert save.get_attribute("type") == "submit"
```

```java
WebElement panel = driver.findElement(By.cssSelector("[data-testid='profile-panel']"));
WebElement save = panel.findElement(By.cssSelector("button[data-action='save']"));
if (!"submit".equals(save.getAttribute("type"))) throw new AssertionError("wrong control");
```

<CodePlayground language="python" title="Run it — accept one locator contract (Python)" code={`CANDIDATES = ["data-testid=checkout-submit","class=css-1a2b3c","xpath=//div[4]/button"]
EXPECTED = "data-testid=checkout-submit"

def choose(candidates):
    accepted = [value for value in candidates if value == EXPECTED]
    if len(accepted) != 1:
        raise AssertionError(f"expected one accepted locator, got {len(accepted)}")
    return accepted[0]

selected = choose(CANDIDATES)
accepted = selected == EXPECTED
wrong_rejected = all(value != selected for value in CANDIDATES if value != EXPECTED)
assert accepted, "the intended locator contract must be selected"
assert wrong_rejected, "every accidental locator must be rejected"
print(f"SELECT {selected}")
print("RULE product-owned unique contract")
print("RESULT accepted=true wrong_rejected=true")`}/>

<CodePlayground language="java" title="Run it — accept one locator contract (Java)" code={`import java.util.ArrayList;
import java.util.List;

public class Main {
    static final String EXPECTED = "data-testid=checkout-submit";
    static String choose(List<String> candidates) {
        List<String> accepted = new ArrayList<>();
        for (String value : candidates) if (value.equals(EXPECTED)) accepted.add(value);
        if (accepted.size() != 1) throw new AssertionError("expected one accepted locator, got " + accepted.size());
        return accepted.get(0);
    }
    public static void main(String[] args) {
        List<String> candidates = List.of("data-testid=checkout-submit", "class=css-1a2b3c", "xpath=//div[4]/button");
        String selected = choose(candidates);
        boolean accepted = selected.equals(EXPECTED);
        boolean wrongRejected = candidates.stream().filter(value -> !value.equals(EXPECTED)).allMatch(value -> !value.equals(selected));
        if (!accepted) throw new AssertionError("the intended locator contract must be selected");
        if (!wrongRejected) throw new AssertionError("every accidental locator must be rejected");
        System.out.println("SELECT " + selected);
        System.out.println("RULE product-owned unique contract");
        System.out.println("RESULT accepted=true wrong_rejected=true");
    }
}`}/>

<FirstTime title="Your mission: defend one locator">
<StepChecklist steps={[
  { text: "State the target", detail: "Write the user or product fact that identifies the intended element." },
  { text: "List candidates", detail: "Record IDs, names, data attributes, semantics, CSS, and XPath options." },
  { text: "Test uniqueness", detail: "Measure count in the correct document, frame, and component scope." },
  { text: "Prove identity", detail: "Check one decisive property before the action." },
]}/>
You now have a locator contract and evidence, not merely a selector string.
</FirstTime>

<WhenItBreaks items={[
  { symptom: "The locator returns no element.", fix: "Check window and frame context, readiness, then whether the contract changed." },
  { symptom: "The locator returns the wrong element.", fix: "Measure all matches, add meaningful scope, and verify identity." },
  { symptom: "A harmless wrapper breaks the selector.", fix: "Remove positional and wrapper dependencies." },
  { symptom: "Another viewport changes the match.", fix: "Inspect responsive DOM or geometry instead of adding a blind fallback." },
]}/>

<WhereToCheck>
- **Elements panel** — live attributes, accessible name, ancestry, and match count.
- **Browsing context** — active window, frame, and component scope.
- **Application source** — owned IDs and data attributes versus generated output.
- **Failure evidence** — selector, count, candidates, viewport, and page state.
</WhereToCheck>

<WorkedExample title="a Save button that became two">
A settings page starts with one Save button. A new panel adds another, so the old first-match query silently clicks the wrong control. The repaired test scopes to the profile panel, locates its owned save action, verifies type=submit, and rejects zero or multiple candidates.
</WorkedExample>

<Quiz question="What makes a locator robust?" options={[
  "It is the longest selector", "It always returns the first match", "It encodes stable intent and proves identity", "It combines CSS and XPath fallbacks"
]} answer={2} explain="Robustness survives irrelevant changes while rejecting absence, ambiguity, and the wrong target." />

<Flashcards cards={[
  { front: "Locator", back: "A strategy and value used to identify elements." },
  { front: "Scope", back: "A stable context that narrows legitimate candidates." },
  { front: "Match count", back: "Evidence distinguishing absent, unique, and ambiguous." },
  { front: "Oracle", back: "A decisive property checked before acting." },
]}/>

<Challenge>
Mutate EXPECTED in both playgrounds to an accidental candidate. The original assertions must reject the change. Then duplicate the intended candidate and require the uniqueness oracle to reject ambiguity.
</Challenge>

<AskCommunity prompt="My locator [value] in [window/frame/component] matched [count]. I expected [identity], observed [attributes], and the DOM change was [change]. Which contract should I strengthen?">
Remove private URLs, customer data, credentials, and session values.
</AskCommunity>

<Resources links={[
  { href: "https://www.selenium.dev/documentation/webdriver/elements/locators/", title: "Selenium — Locator strategies", kind: "reference" },
  { href: "https://www.selenium.dev/documentation/test_practices/encouraged/locators/", title: "Selenium — Locator practices", kind: "reference" },
  { href: "https://www.w3.org/TR/webdriver2/#element-retrieval", title: "W3C WebDriver — Element retrieval", kind: "reference" },
]}/>

<Video href="https://www.youtube.com/watch?v=2u9ov_NMPjk" title="Playwright Tutorial #8 - getbyRole() Locator | Locators in Playwright" minutes={16} />

<Takeaways points={[
  "Locator syntax and durability are different questions.",
  "Stable meaning matters more than CSS-versus-XPath preference.",
  "Match count exposes absence and ambiguity.",
  "Scope and a post-location oracle prevent wrong-element actions.",
  "Real Selenium stays fenced; playgrounds model the decision.",
]}/>

<Complete xp={10} />

