---
title: "REST Assured setup & first test"
summary: "Add REST Assured to a Java test project, set a base URI, send one request, and make the response prove its status instead of merely printing JSON and hoping for applause."
tags: ["api-test-automation", "rest-assured-java", "track-d"]
updated: "2026-07-17"
related:
  - "api-test-automation/rest-assured-java/given-when-then-style"
  - "api-test-automation/rest-assured-java/validating-json-and-status"
  - "api-testing-fundamentals/postman-and-curl/curl-basics"
---

<Hook>
Installing a library is not an API test. It is shelving a tool. Your first useful REST Assured test
must send a known request and fail loudly when the response breaks its promise.
</Hook>

<Callout type="analogy">
A laboratory bench can hold impressive glassware and still produce no evidence. Maven puts the
glassware on the bench; the request, assertion, and test runner turn it into an experiment.
</Callout>

<Term define="REST Assured is a Java DSL for testing HTTP services. In a JUnit or TestNG test it builds a request, sends it, exposes the response, and applies assertions through a fluent API.">REST Assured</Term>

## The smallest honest setup

Add `io.rest-assured:rest-assured` as a test-scoped dependency and use a current version from the
project's release page. Pair it with JUnit 5 or TestNG; REST Assured does not discover tests itself.

```java
import static io.restassured.RestAssured.given;
import static org.hamcrest.Matchers.equalTo;

@Test
void health_is_up() {
    given().baseUri("http://localhost:4000")
    .when().get("/health")
    .then().statusCode(200).body("status", equalTo("ok"));
}
```

<Callout type="tip">
Keep the base URI configurable. Hard-coding production into every test is not bravery; it is an
incident report waiting for a timestamp.
</Callout>

<Callout type="mistake">
Calling `prettyPrint()` without an assertion. Pretty JSON proves only that the printer works.
</Callout>

{/* IMAGE: a chemistry laboratory bench with glassware and equipment arranged for experiments */}
<HotspotImage
  src="/notes/api-test-automation/rest-assured-java/setup-and-first-test.jpg"
  alt="Chemistry laboratory bench with glassware, bottles, and instruments"
  credit="Laboratory bench — Cjp24, Wikimedia Commons, CC BY-SA 3.0"
  creditHref="https://commons.wikimedia.org/wiki/File:Laboratory_bench.jpg"
  pins={[
    { x: 34, y: 58, label: "Dependency", desc: "The equipment must be present before a test can use the DSL." },
    { x: 62, y: 38, label: "Known request", desc: "One controlled experiment starts with a named target and input." },
    { x: 77, y: 70, label: "Assertion", desc: "The reading becomes evidence only when compared with an expected result." },
  ]}
/>

<FlowAnimation title="From dependency to result" intervalMs={1600} nodes={[
  { label: "Add test dependency", desc: "Maven or Gradle places REST Assured on the test classpath." },
  { label: "Choose base URI", desc: "Point at a disposable local or test environment." },
  { label: "Send request", desc: "The HTTP method and path identify the behavior under test." },
  { label: "Assert response", desc: "Status and body checks decide pass or fail." },
]}/>

<CodePlayground language="python" title="Run it — the anatomy of a first API test (Python)" code={`response = {"status": 200, "json": {"status": "ok"}}
checks = [
    ("status is 200", response["status"] == 200),
    ("body status is ok", response["json"]["status"] == "ok"),
]
for name, passed in checks:
    print(f"{name}: {'PASS' if passed else 'FAIL'}")
print(f"result: {sum(p for _, p in checks)}/{len(checks)} checks passed")

# status is 200: PASS
# body status is ok: PASS
# result: 2/2 checks passed`}/>

<CodePlayground language="java" title="Run it — the same contract (Java)" code={`import java.util.*;
public class Main {
  public static void main(String[] args) {
    int status = 200;
    Map<String, String> body = Map.of("status", "ok");
    boolean statusOk = status == 200;
    boolean bodyOk = "ok".equals(body.get("status"));
    System.out.println("status is 200: " + (statusOk ? "PASS" : "FAIL"));
    System.out.println("body status is ok: " + (bodyOk ? "PASS" : "FAIL"));
    System.out.println("result: " + ((statusOk ? 1 : 0) + (bodyOk ? 1 : 0)) + "/2 checks passed");
  }
}

/* status is 200: PASS
   body status is ok: PASS
   result: 2/2 checks passed */`}/>

<FirstTime title="Your mission: make one response earn a pass"><StepChecklist steps={[
  { text: "Create a Java test project with JUnit 5 or TestNG", detail: "Confirm one empty test is discovered before adding HTTP code." },
  { text: "Add REST Assured as a test dependency", detail: "Use the version shown by the official release page, not a fossil copied from a blog." },
  { text: "Test a disposable health endpoint", detail: "Assert both status 200 and one stable body field." },
  { text: "Break one expectation deliberately", detail: "Read the actual failure before restoring the correct value." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "The test runner reports zero tests.", fix: "Check the JUnit/TestNG engine, annotation import, filename, and test task before blaming REST Assured." },
  { symptom: "Connection refused.", fix: "Verify scheme, host, port, and that the test service is listening from the same environment as the runner." },
  { symptom: "The body matcher cannot parse JSON.", fix: "Inspect Content-Type and raw body; an HTML proxy error is not JSON wearing a convincing hat." },
]}/>

<WhereToCheck>
- The test runner report for discovery and assertion failures.
- Request/response logging for method, URL, headers, and body.
- The service health endpoint and environment configuration.
</WhereToCheck>

<WorkedExample title="a first test that catches the useful failure">
The health endpoint returns `200` but its body changes from `{"status":"ok"}` to
`{"status":"degraded"}`. A status-only test smiles and waves it through. The body assertion fails
and identifies the behavioral change immediately.
</WorkedExample>

<Quiz question="Why should the first test assert a stable body field as well as status 200?" options={["To make the test longer", "Because 200 alone can accompany the wrong payload or degraded state", "Because REST Assured cannot read status codes", "To replace service monitoring"]} answer={1} explain="A successful HTTP status does not prove the response body satisfies the contract." />

<Flashcards cards={[
  { front: "Who discovers the test?", back: "JUnit or TestNG; REST Assured supplies the HTTP testing DSL." },
  { front: "Why test scope?", back: "REST Assured is normally needed by tests, not production runtime code." },
  { front: "First two assertions", back: "Expected status plus one stable, meaningful response field." },
  { front: "Why break it once?", back: "To prove the test can fail and learn what its diagnostic output looks like." },
]}/>

<Challenge>Make the base URI come from an environment variable with a safe local default, then prove a bad URI and a bad body expectation produce distinguishable failures.</Challenge>

<AskCommunity prompt="My first REST Assured test fails with `[exact error]` against `[method + URL]`; here is the dependency version and sanitized response log.">Include the runner, Java version, and full assertion message. Remove tokens before posting.</AskCommunity>

<Resources links={[
  { href: "https://github.com/rest-assured/rest-assured/wiki/GettingStarted", title: "REST Assured — official getting started guide", kind: "read" },
  { href: "https://github.com/rest-assured/rest-assured/releases", title: "REST Assured — official releases", kind: "read" },
]}/>

<Video href="https://www.youtube.com/watch?v=eesqK59rhGA" title="The TechCave — HTTP and the request-response cycle" minutes={9} />

<Takeaways points={[
  "REST Assured builds and validates HTTP interactions; JUnit or TestNG runs the test.",
  "A first test needs a request and assertions, not merely a printed response.",
  "Keep environment addresses configurable and default to a disposable target.",
  "Assert status and a stable body field so a polite but wrong response still fails.",
]}/>

<Complete xp={10} />
