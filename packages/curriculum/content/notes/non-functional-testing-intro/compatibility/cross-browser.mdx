---
title: "Cross-browser"
summary: "Build a risk-based browser matrix, test engines and features that matter to users, and distinguish graceful fallback from silent incompatibility."
tags: ["non-functional-testing-intro", "compatibility", "track-c"]
updated: "2026-07-18"
related:
  - "non-functional-testing-intro/compatibility/cross-device"
  - "non-functional-testing-intro/compatibility/os-and-versions"
  - "playwright/parallel-and-cross-browser/projects-and-browsers"
---

<Hook>
The web has shared standards, but users do not run "the standard"—they run a browser engine, version,
operating system, settings, and extensions. A checkout that works in one combination can lose its date
picker, layout, or payment callback in another while every backend test remains green.
</Hook>

<Callout type="analogy">
Cross-browser testing is checking a key cut by several locksmiths. The design is shared, yet tiny
differences in manufacturing can stop one lock. Test the doors your users actually open, not every lock
ever made and not only the one on your desk.
</Callout>

<Term define="Cross-browser testing evaluates important web journeys across a documented, risk-based matrix of browser engines, browser versions, and operating-system combinations. Its goal is an acceptable experience and intentional fallback, not pixel-identical rendering everywhere.">Cross-browser testing</Term>

## Build the matrix from evidence

Use supported-product policy, current analytics, customer commitments, and feature risk. Include the
major engines relevant to the audience—Blink, WebKit, and Gecko—because multiple browser brands can
share an engine. Test critical journeys first, then layout, input, media, storage, permissions, and
feature fallbacks. Consult MDN Browser Compatibility Data, but confirm behavior in the real matrix.

<Callout type="tip">
Record browser, full version, engine, OS, viewport, feature flags, and reproduction. "Broken in Safari"
is a lead; "Safari 18.5 on macOS 15.5, checkout date input rejects keyboard entry" is evidence.
</Callout>

<Callout type="mistake">
Do not confuse identical screenshots with compatibility. Native controls, fonts, and antialiasing may
differ acceptably. Prioritize missing actions, unreadable content, data loss, inaccessible behavior, and
unannounced fallback over harmless pixel variation.
</Callout>

{/* IMAGE: a browser compatibility result showing an 84 percent score and a detailed list of passed and failed web features */}
<HotspotImage
  src="/notes/non-functional-testing-intro/compatibility/cross-browser.jpg"
  alt="A Firefox browser compatibility test result showing 84 percent and a list of passing and failing web features"
  credit="Web Compatibility Test for Mobile Browsers 2 with Firefox 115 — StreetcarEnjoyer, Wikimedia Commons, CC0"
  creditHref="https://commons.wikimedia.org/wiki/File:Web_Compatability_Test_for_Mobile_Browsers_2_with_Desktop_Firefox_115.png"
  pins={[
    { x: 50, y: 18, label: "A summary is not the user journey", desc: "An overall score can hide a failed feature that a critical flow depends on." },
    { x: 50, y: 40, label: "Exact browser identity", desc: "Capture the full user agent, browser version, engine, and OS instead of a vague brand name." },
    { x: 70, y: 67, label: "Feature-level failures", desc: "Investigate whether failed capabilities have an intentional fallback and whether the product actually uses them." },
    { x: 35, y: 75, label: "Passing capabilities", desc: "Support tables guide selection; only a real end-to-end task establishes product behavior." },
  ]}
/>

<FlowAnimation title="A risk-based browser loop" intervalMs={1600} nodes={[
  { label: "Collect support policy, analytics, and critical flows", desc: "Matrix choices must be traceable to users and business risk." },
  { label: "Cover relevant engines and versions", desc: "Choose meaningful combinations instead of multiplying brands that share the same engine." },
  { label: "Run smoke journeys, then targeted feature checks", desc: "Start with value and safety; investigate layout and API differences where the product depends on them." },
  { label: "Document fallback and refresh the matrix", desc: "Browser usage and support change, so date the evidence and retire rows intentionally." },
]}/>

<CodePlayground language="python" title="A browser-matrix oracle (Python)" code={`checks = {
    "blink_checkout": True,
    "webkit_checkout": True,
    "gecko_checkout": True,
    "fallback_documented": True,
}
for name, passed in checks.items():
    print(name + "=" + ("PASS" if passed else "FAIL"))
result = "PASS" if all(checks.values()) else "FAIL"
assert result == "PASS", "browser matrix rejected"
print("RESULT=" + result)`}/>

<CodePlayground language="java" title="A browser-matrix oracle (Java)" code={`import java.util.LinkedHashMap;
import java.util.Map;
public class Main {
    public static void main(String[] args) {
        Map<String, Boolean> checks = new LinkedHashMap<>();
        checks.put("blink_checkout", true);
        checks.put("webkit_checkout", true);
        checks.put("gecko_checkout", true);
        checks.put("fallback_documented", true);
        boolean ok = true;
        for (var e : checks.entrySet()) { System.out.println(e.getKey() + "=" + (e.getValue() ? "PASS" : "FAIL")); ok &= e.getValue(); }
        String result = ok ? "PASS" : "FAIL";
        if (!result.equals("PASS")) throw new AssertionError("browser matrix rejected");
        System.out.println("RESULT=" + result);
    }
}`}/>

<FirstTime title="Create a three-engine smoke matrix">
<StepChecklist steps={[
  { text: "Name supported browsers from evidence", detail: "Use policy, analytics, customer needs, and current feature support; date the matrix." },
  { text: "Choose one critical journey", detail: "Run the same data and assertions in representative Blink, WebKit, and Gecko combinations." },
  { text: "Inspect browser-specific signals", detail: "Check console, network, feature support, permissions, controls, and intentional fallbacks." },
  { text: "File exact environment evidence", detail: "Include full versions, OS, viewport, steps, expected result, actual result, and impact." },
]}/>
</FirstTime>

<WhenItBreaks items={[
  { symptom: "A feature works in Chrome but is absent elsewhere.", fix: "Check standards status and MDN compatibility data, then implement or test an intentional feature-detection fallback rather than user-agent guessing." },
  { symptom: "Screenshots differ by a few pixels.", fix: "Test readability, operation, overflow, and task completion. Treat harmless font or native-control variation as acceptable unless the product specifies otherwise." },
  { symptom: "The matrix grows without bound.", fix: "Collapse equivalent low-risk combinations, prioritize engines and supported versions, and schedule deeper rows by usage and failure impact." },
]}/>

<WhereToCheck>
- MDN Browser Compatibility Data and current browser release/support documentation.
- Console, network, feature detection, permissions, storage, media, and native inputs.
- Product analytics and written support policy, dated with the test run.
- [[non-functional-testing-intro/compatibility/cross-device]] for hardware and interaction differences beyond the browser.
</WhereToCheck>

<WorkedExample title="a date field with no safe fallback">
1. Checkout uses a new date API and passes in the team's default browser.
2. A supported WebKit combination lacks the behavior; the field appears but cannot submit a valid date.
3. The tester reports exact versions, console evidence, task impact, and the absence of fallback.
4. Feature detection selects a validated text fallback, and the journey is rerun across all matrix rows.
</WorkedExample>

<Quiz question="What is the best basis for a browser matrix?" options={[
  "Every browser ever released", "Only the QA team's default browser", "Supported policy, user analytics, engine coverage, critical flows, and feature risk", "Pixel-identical screenshots"
]} answer={2} explain="A defensible matrix is risk-based and dated. It covers relevant engines and users without pretending exhaustive combinations are possible." />

<Flashcards cards={[
  { front: "Engine coverage", back: "Browser brands may share Blink, WebKit, or Gecko; engine diversity often matters more than brand count." },
  { front: "Compatibility target", back: "Acceptable task completion and intentional fallback, not identical pixels everywhere." },
  { front: "Reproduction", back: "Browser and full version, engine, OS, viewport, settings, steps, result, and impact." },
]}/>

<Challenge>Build a dated three-engine matrix for one critical journey, justify each row, and document one intentional fallback.</Challenge>
<AskCommunity prompt="Which browser-specific failure looked harmless in a screenshot but blocked a real user journey?" />

<Resources links={[
  { href: "https://developer.mozilla.org/en-US/docs/Learn_web_development/Extensions/Testing/Introduction", title: "MDN — Introduction to Cross-Browser Testing", kind: "read" },
  { href: "https://developer.mozilla.org/en-US/docs/MDN/Writing_guidelines/Page_structures/Compatibility_tables", title: "MDN — Browser Compatibility Data", kind: "read" },
  { href: "https://www.youtube.com/watch?v=fiPzpMUhaKE", title: "The Testing Academy — Cross Browser Testing - Ultimate Guide (Start to Finish) [With Checklist]", kind: "watch" },
]}/>
<Video href="https://www.youtube.com/watch?v=fiPzpMUhaKE" title="Cross Browser Testing - Ultimate Guide (Start to Finish) [With Checklist]" minutes={14} />

<Takeaways points={[
  "Build and date the matrix from supported users, engines, critical journeys, and feature risk.",
  "Test outcomes and fallbacks rather than demanding pixel identity.",
  "Use compatibility data to plan, then verify the real product in exact combinations.",
  "A reproducible browser finding names full environment and user impact.",
]}/>
<Complete xp={10} />
