---
title: "Test pyramids for APIs"
summary: "Build most confidence below the browser, keep a focused API layer for service behavior, and reserve full end-to-end journeys for risks only the whole system can reveal."
tags: ["api-test-automation", "real-world-api-suites", "track-d"]
updated: "2026-07-17"
related:
  - "api-test-automation/real-world-api-suites/data-setup-via-api"
  - "api-test-automation/real-world-api-suites/full-api-suite-on-buggyapi"
  - "levels-and-types-of-testing/test-levels/integration"
---

<Hook>If every rule needs a browser, the test suite has hired a chauffeur to cross the kitchen. API checks sit where rich behavior is observable without paying the full UI tax.</Hook>

<Callout type="analogy">A pyramid carries most weight in its broad base. Tests work similarly: many focused checks below, fewer service integrations, and a thin cap of expensive whole-system journeys.</Callout>

<Term define="An API test pyramid is a portfolio heuristic: many fast unit/component tests support a substantial but smaller service/API layer, with few full end-to-end checks. It is not a mandated percentage; architecture, risk, feedback time, and maintenance cost determine the useful shape.">API test pyramid</Term>

## Put each risk at the cheapest revealing layer

Pure validation rules belong in unit tests. HTTP serialization, authentication, persistence boundaries, and endpoint contracts earn API tests. A handful of browser or cross-service journeys prove wiring the API layer cannot see. Duplication across every layer adds runtime without automatically adding information.

<Callout type="tip">Ask “what is the lowest layer that can fail for the right reason?” before adding another end-to-end test.</Callout>

<Callout type="mistake">Do not turn the pyramid into a quota. Ten fragile API checks are not improved by inventing ninety trivial unit tests to reach a slide-deck ratio.</Callout>

<HotspotImage src="/notes/api-test-automation/real-world-api-suites/test-pyramids-for-apis.jpg" alt="The Great Pyramid of Giza rising above a modern city street in a black-and-white photograph" credit="Great Pyramid Giza — Ireneusz Jerzy Borysiewicz, CC BY-SA 4.0, via Wikimedia Commons" creditHref="https://commons.wikimedia.org/wiki/File:Great_Pyramid_Giza.jpg" pins={[
  { x: 19, y: 45, label: "Broad stone base", desc: "Like fast focused checks, the widest layer carries most of the structure's weight." },
  { x: 29, y: 27, label: "Narrower middle", desc: "The API layer is substantial but more selective: contracts, auth, HTTP behavior, and persistence wiring." },
  { x: 34, y: 7, label: "Tiny apex", desc: "Only critical end-to-end journeys pay the highest setup and diagnosis cost." },
]}/>

<FlowAnimation title="Place a risk at the right layer" intervalMs={1400} nodes={[
  { label: "Name the risk", desc: "Example: unauthorized users must not create tickets." },
  { label: "Find the lowest revealing layer", desc: "Middleware unit test may cover branching; HTTP test proves wiring." },
  { label: "Add only missing evidence", desc: "Avoid repeating identical assertions at every layer." },
  { label: "Measure feedback", desc: "Runtime, flake rate, and diagnostic value reshape the portfolio." },
]}/>

<CodePlayground language="python" title="Run it - allocate risks by cheapest revealing layer (Python)" code={`risks = {"title validation": "unit", "auth header wiring": "api", "browser login journey": "e2e", "pagination contract": "api"}
for layer in ("unit", "api", "e2e"):
    names = [risk for risk, owner in risks.items() if owner == layer]
    print(f"{layer}: {len(names)} -> {', '.join(names)}")`}/>

<CodePlayground language="java" title="Run it - allocate the same risks (Java)" code={`import java.util.*;
public class Main {
  public static void main(String[] args) {
    Map<String,String> risks = new LinkedHashMap<>();
    risks.put("title validation","unit"); risks.put("auth header wiring","api");
    risks.put("browser login journey","e2e"); risks.put("pagination contract","api");
    for (String layer : List.of("unit","api","e2e")) {
      List<String> names = risks.entrySet().stream().filter(e -> e.getValue().equals(layer)).map(Map.Entry::getKey).toList();
      System.out.println(layer + ": " + names.size() + " -> " + String.join(", ", names));
    }
  }
}`}/>

<FirstTime title="Your mission: map twelve API risks"><StepChecklist steps={[
  { text: "List business, protocol, contract, and journey risks", detail: "Start from failure impact, not existing test folders." },
  { text: "Assign the lowest revealing layer", detail: "Unit, component, API integration, contract, or end-to-end." },
  { text: "Mark intentional overlap", detail: "Overlap needs a reason such as contract plus runtime wiring." },
  { text: "Set a feedback budget", detail: "Decide what must finish on a pull request versus nightly." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "The API suite takes longer than the UI suite.", fix: "Audit repeated setup, broad data resets, network dependencies, and scenarios that belong below HTTP." },
  { symptom: "Unit tests pass but deployed API is broken.", fix: "Add focused integration checks for routing, serialization, middleware, and persistence wiring." },
  { symptom: "The same defect fails dozens of tests.", fix: "Remove redundant journeys and keep one strong assertion at each layer that adds distinct evidence." },
]}/>

<WhereToCheck>
- Risk-to-layer map and intentional overlaps.
- Runtime, flake rate, setup cost, and first-failure diagnostic clarity by layer.
- Gaps around auth, schema, persistence, and critical journeys.
</WhereToCheck>

<WorkedExample title="ticket creation at three useful depths">
A unit test covers title-length rules. An API test posts an authenticated ticket and verifies status, response shape, and persisted retrieval. One browser journey proves the UI sends a valid request. The three checks overlap on the feature but fail for different reasons and answer different questions.
</WorkedExample>

<Quiz question="Which claim best describes a test pyramid?" options={["A fixed 70/20/10 quota", "A heuristic for placing risk at cheaper, faster layers while retaining necessary integration evidence", "A ban on API tests", "A CI report format"]} answer={1} explain="The pyramid guides portfolio economics; it is not a universal percentage or a reason to skip real integration risks."/>

<Flashcards cards={[
  { front: "API layer earns its place by testing?", back: "HTTP contracts, auth, serialization, persistence wiring, and service behavior." },
  { front: "Why few end-to-end tests?", back: "They cost more to set up, run, and diagnose, so reserve them for risks only the full system reveals." },
  { front: "Is the pyramid a quota?", back: "No. It is a risk and feedback heuristic." },
]}/>

<Challenge>Take one feature with ten current end-to-end cases. Reassign each assertion to the lowest revealing layer and keep only journeys that prove unique wiring.</Challenge>

<AskCommunity prompt="Our API portfolio is slow around `[domain]`; current layers, runtimes, and duplicate assertions are `[details]`.">Bring measurements before proposing a prettier triangle.</AskCommunity>

<Resources links={[
  { href: "https://testing.googleblog.com/2015/04/just-say-no-to-more-end-to-end-tests.html", title: "Google Testing Blog — Just Say No to More End-to-End Tests", kind: "read" },
  { href: "https://martinfowler.com/articles/practical-test-pyramid.html", title: "Martin Fowler — The Practical Test Pyramid", kind: "read" },
]}/>

<Video href="https://www.youtube.com/watch?v=90auAVyZswc" title="Building Quality Gates in DevOps CI/CD Pipelines — DheerajTechInsight" minutes={31} />

<Takeaways points={[
  "Place each risk at the lowest layer that can fail for the right reason.",
  "Use API tests for protocol and service wiring, not every pure rule.",
  "Keep only a thin set of whole-system journeys with unique evidence.",
  "Reshape the portfolio using runtime, flake, maintenance, and diagnosis data—not quotas.",
]}/>

<Complete xp={10} />
