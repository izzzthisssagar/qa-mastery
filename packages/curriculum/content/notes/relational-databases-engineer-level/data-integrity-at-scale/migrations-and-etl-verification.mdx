---
title: "Migrations and ETL Verification"
summary: "Verify schema migrations and ETL movement with phased gates, reconciliation, invariants, and rollback evidence."
tags: ["postgresql", "migrations", "etl", "data-reconciliation"]
updated: "2026-07-17"
related: ["relational-databases-engineer-level/data-integrity-at-scale/constraints-and-referential-integrity", "relational-databases-engineer-level/data-integrity-at-scale/finding-orphans-and-duplicates", "relational-databases-engineer-level/data-integrity-at-scale/auditing-data-changes"]
---

<Hook>A migration that exits zero can still lose, duplicate, truncate, or mis-map data. Verification must prove structure, meaning, completeness, and operability at the same extraction boundary.</Hook>

<Callout type="analogy">ETL is container shipping. Counting containers at departure and arrival helps, but seals, manifests, weights, and destination scans prove the same cargo arrived intact.</Callout>

## Gate the change in layers

Separate schema change, backfill, forward-write enforcement, validation, and cleanup when risk or scale demands it. Reconcile source and target using a shared <Term define="A stable extraction boundary that makes two data sets comparable.">watermark</Term> or consistent snapshot. Counts are necessary but weak; add key-set differences, grouped totals, null distributions, checksums, referential checks, and sampled row comparisons.

<HotspotImage src="/notes/relational-databases-engineer-level/data-integrity-at-scale/migrations-and-etl-verification.jpg" alt="A container ship illustrating controlled data movement" credit="Photo: Syukrimsw — Wikimedia Commons, CC BY-SA 4.0" creditHref="https://commons.wikimedia.org/wiki/File:ContainerShip.jpg" pins={[
  { x: 28, y: 49, label: "Source manifest", desc: "Define extraction boundary, keys, counts, and aggregates before movement." },
  { x: 55, y: 36, label: "Transformation", desc: "Mapping rules, defaults, type conversions, and rejected rows require explicit evidence." },
  { x: 79, y: 57, label: "Arrival reconciliation", desc: "Compare key sets, distributions, totals, constraints, and samples at the same watermark." },
]}/>

<Callout type="tip">Store every verification query and result with migration version, start/end timestamps, source watermark, row counts, and rejection count. A dashboard without a boundary is not reconcilable evidence.</Callout>

<Callout type="mistake">Comparing a live source count at 10:00 with a target count at 10:20. New writes make both numbers valid and the comparison meaningless.</Callout>

<FlowAnimation title="Migration gates" intervalMs={1250} nodes={[
  { label: "Baseline", desc: "Capture schema, volume, anomalies, performance, and a stable extraction boundary." },
  { label: "Move", desc: "Apply compatible DDL and idempotent batches with checkpoints and rejection handling." },
  { label: "Reconcile", desc: "Compare keys, totals, distributions, constraints, and samples at the same boundary." },
  { label: "Cut over", desc: "Observe application behavior, retain rollback/forward-fix evidence, then clean legacy paths." },
]}/>

<CodePlayground language="python" title="Reconcile an ETL batch" code={`source = {"A": 120, "B": 80, "C": 50}
target = {"A": 120, "B": 80, "C": 50}

missing = sorted(source.keys() - target.keys())
extra = sorted(target.keys() - source.keys())
mismatched = sorted(k for k in source.keys() & target.keys() if source[k] != target[k])

assert not missing and not extra and not mismatched
assert sum(source.values()) == sum(target.values())
print({"rows": len(target), "total": sum(target.values()), "status": "reconciled"})`}/>

<CodePlayground language="java" title="Reconcile the batch in Java" code={`import java.util.*;
class Main {
  public static void main(String[] args) {
    Map<String,Integer> source = Map.of("A",120,"B",80,"C",50);
    Map<String,Integer> target = Map.of("A",120,"B",80,"C",50);
    if (!source.keySet().equals(target.keySet())) throw new AssertionError("key mismatch");
    for (String key : source.keySet())
      if (!source.get(key).equals(target.get(key))) throw new AssertionError(key);
    System.out.println("reconciled keys: " + new TreeSet<>(target.keySet()));
  }
}`}/>

<FirstTime title="Verify one migration"><StepChecklist steps={[
  { text: "Freeze the comparison boundary", detail: "Use a snapshot, watermark, or immutable batch identifier." },
  { text: "Verify schema and mapping", detail: "Check types, defaults, nullability, indexes, constraints, and transformation rules." },
  { text: "Reconcile content", detail: "Compare key sets, grouped totals, distributions, rejects, and deterministic samples." },
  { text: "Prove recovery", detail: "Rehearse retry, resume, rollback or forward-fix, and application compatibility." },
]}/></FirstTime>

<WhenItBreaks items={[
  { symptom: "Counts match but money totals do not.", fix: "Add domain aggregates and per-key comparisons; duplicates can cancel missing rows at count level." },
  { symptom: "A backfill rerun duplicates target rows.", fix: "Use deterministic source keys, upsert or checkpoint semantics, and test the same batch twice." },
  { symptom: "DDL is correct but deployment stalls writes.", fix: "Rehearse locks and duration at representative scale; split schema, backfill, validation, and cleanup phases." },
]}/>

<WhereToCheck>Keep migration version/checksum, DDL state, lock waits, batch checkpoints, `COPY` row counts, rejection tables, source/target key diffs, aggregates, constraint validation, query plans, and application error rate.</WhereToCheck>

<WorkedExample title="Moving legacy orders into a typed table">At watermark W, record source keys, row count, total amount, null/status distributions, and orphan count. Load in keyed batches. Compare target at W, inspect every reject, validate constraints, rerun one batch to prove idempotency, then run old and new reads in shadow before cutover.</WorkedExample>

<Quiz question="Why can equal source and target row counts still hide corruption?" options={["Counts are always rounded", "A missing row and a duplicate can cancel, while values may also be transformed incorrectly", "PostgreSQL cannot count", "Indexes change row counts"]} answer={1} explain="Counts measure volume, not identity or meaning; use key-set and domain reconciliation." />

<Flashcards cards={[
  { front: "Watermark", back: "A stable extraction boundary that makes source and target comparisons refer to the same data." },
  { front: "Reconciliation", back: "Evidence that identities, values, relationships, and aggregates agree across movement." },
  { front: "Idempotent batch", back: "Rerunning the same batch produces no additional unintended effect." },
]}/>

<Challenge>Design a verification ledger for one migration: boundary, schema gates, key diff, three domain aggregates, distributions, rejects, constraint checks, retry proof, and cutover rollback signal.</Challenge>

<AskCommunity prompt="Migration/version: [id]. Boundary: [watermark]. Source/target keys and aggregates: [evidence]. Rejects: [count]. Retry result: [evidence]. Which reconciliation gap remains?">Include whether writes continued during extraction; it determines whether your comparison is valid.</AskCommunity>

<Resources links={[
  { href: "https://www.postgresql.org/docs/current/ddl-alter.html", title: "PostgreSQL — Modifying Tables", kind: "read" },
  { href: "https://www.postgresql.org/docs/current/sql-copy.html", title: "PostgreSQL — COPY", kind: "read" },
  { href: "https://www.postgresql.org/docs/current/sql-altertable.html", title: "PostgreSQL — ALTER TABLE", kind: "read" },
]}/>

<Video href="https://www.youtube.com/watch?v=qw--VYLpxG4" title="Learn PostgreSQL — Full Course for Beginners" minutes={260} />

<Takeaways points={["Compare source and target at one explicit boundary.", "Counts need key-set, aggregate, distribution, relationship, and sample checks.", "Phase risky changes and verify locks at representative scale.", "Test retry and recovery before cutover, not during an incident."]}/>

<Complete xp={10} />
