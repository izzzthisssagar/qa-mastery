---
title: "Artifacts"
summary: "CI artifacts preserve the reports, traces, screenshots, videos, logs, and build outputs needed to explain a run after its temporary runner has disappeared, with deliberate upload and retention rules."
tags: ["ci-cd", "artifacts", "debugging", "track-d"]
updated: "2026-07-17"
related:
  - "automation-in-cicd/running-tests-in-ci/headless-mode"
  - "automation-in-cicd/scheduling-and-reporting/publishing-reports"
  - "playwright/tracing-and-debugging/screenshots-and-video"
---

<Hook>
A CI runner is disposable. When its job ends, the filesystem that contained the failed test's trace
and screenshot can vanish with it. If the team uploads only the green checkmark, the best evidence
often disappears seconds before someone begins investigating.
</Hook>

<Callout type="analogy">
An archive is useful because folders are labelled, preserved for a known period, and retrievable by a
future person who was not present when they were filed. A heap of unnamed CI output is not an archive;
it is delayed deletion.
</Callout>

<Term define="A CI artifact is a file or collection of files uploaded from a job's temporary workspace and retained by the CI system after the job ends. Artifacts commonly include test reports, traces, screenshots, videos, logs, coverage data, and packaged builds. They differ from dependency caches: artifacts are evidence or outputs for people and later jobs, while caches are reusable inputs intended to make future runs faster.">CI artifact</Term>

## Preserve evidence deliberately

An artifact policy answers:

- **What:** upload the smallest evidence that explains or consumes the run.
- **When:** use an always-run upload step so failed tests still publish diagnostics.
- **Which revision:** name and metadata must connect evidence to workflow, job, attempt, and commit.
- **How long:** retain failure evidence long enough for triage without storing it forever.
- **Who can read it:** reports may contain URLs, user data, tokens, or screenshots; access is a security decision.

<Callout type="tip">
Upload one self-contained report directory rather than scattered files with broken relative links.
Include a small metadata file containing the commit SHA, attempt, browser, environment, and test command.
</Callout>

<Callout type="mistake">
Using a cache as the only home for failure evidence. Caches can be evicted, overwritten, or restored
by an unrelated run; they are an optimization, not an audit trail.
</Callout>

<HotspotImage
  src="/notes/automation-in-cicd/running-tests-in-ci/artifacts.jpg"
  alt="Tall shelves densely filled with labelled paper file folders in an archive"
  credit="Shelves of file folders — Alex Gorzen, CC BY-SA 2.0"
  creditHref="https://commons.wikimedia.org/wiki/File:Shelves-of-file-folders.jpg"
  pins={[
    { x: 25, y: 34, label: "Named collection", desc: "Artifact names should identify suite, browser, revision, and attempt without opening every archive." },
    { x: 67, y: 46, label: "Retained evidence", desc: "Upload before the disposable runner is destroyed, including on failed or cancelled test steps where possible." },
    { x: 48, y: 73, label: "Retrievable structure", desc: "Keep a report's assets together so links, traces, and screenshots still open after download." },
    { x: 83, y: 20, label: "Controlled access", desc: "Screenshots and logs can expose sensitive data; retention and permissions belong in the design." },
  ]}
/>

<FlowAnimation title="From failed test to usable artifact" intervalMs={1600} nodes={[
  { label: "Test fails", desc: "The runner writes a report and diagnostic files to known paths." },
  { label: "Metadata captured", desc: "Revision, run attempt, environment, and tool versions travel with the evidence." },
  { label: "Sensitive content checked", desc: "Tokens and unnecessary personal data are redacted or never recorded." },
  { label: "Upload runs always", desc: "A conditional upload executes even though the test step is red." },
  { label: "Retention applies", desc: "The provider keeps the archive for an intentional investigation window." },
  { label: "Investigator downloads", desc: "A teammate can open the self-contained report after the runner is gone." },
]} />

<CodePlayground language="python" title="Run it — choose artifacts by outcome (Python)" code={```files = {
    "html-report": 8,
    "trace": 14,
    "video": 40,
    "dependency-cache": 600,
}
failed = True
selected = ["html-report", "trace"] + (["video"] if failed else [])
print("upload:", ", ".join(selected))
print("total MB:", sum(files[name] for name in selected))
print("cache is evidence:", "dependency-cache" in selected)```} />

<CodePlayground language="java" title="Run it — choose artifacts by outcome (Java)" code={```import java.util.*;

public class Main {
    public static void main(String[] args) {
        var sizes = Map.of("html-report", 8, "trace", 14, "video", 40, "dependency-cache", 600);
        boolean failed = true;
        var selected = new ArrayList<>(List.of("html-report", "trace"));
        if (failed) selected.add("video");
        int total = selected.stream().mapToInt(sizes::get).sum();
        System.out.println("upload: " + String.join(", ", selected));
        System.out.println("total MB: " + total);
        System.out.println("cache is evidence: " + selected.contains("dependency-cache"));
    }
}```} />

<FirstTime title="Your mission: recover evidence after the runner disappears">
<StepChecklist steps={[
  { text: "Configure a known report and trace directory", detail: "Keep paths deterministic so the upload step does not guess with a broad wildcard." },
  { text: "Make artifact upload run on failure", detail: "Use the provider's always condition while preserving the test step's red status." },
  { text: "Trigger one deliberate failure", detail: "Confirm the job is red and the artifact is still present." },
  { text: "Download it in a clean directory", detail: "Open the report and trace as another teammate would; verify relative assets and revision metadata." },
]} />
Evidence is real only when someone can retrieve and interpret it later.
</FirstTime>

<WhenItBreaks items={[
  { symptom: "No artifact appears on a failed job.", fix: "The upload probably inherited success-only execution or the path never existed. Use an always condition and print a bounded directory listing before upload." },
  { symptom: "The HTML opens but screenshots and styles are missing.", fix: "Upload the whole self-contained report directory while preserving its internal paths." },
  { symptom: "Artifact storage grows without limit.", fix: "Record size by suite, capture rich media on failure, compress sensible formats, and set explicit retention." },
  { symptom: "A log artifact exposes a credential.", fix: "Revoke it immediately, restrict access, remove unsafe logging, and treat artifacts as sensitive repository data." },
]} />

<WhereToCheck>
- **Upload-step condition** — it must execute after a failing test step.
- **Resolved path and file count** — distinguish "nothing generated" from "wrong upload glob."
- **Artifact name and metadata** — connect it to SHA, job, browser, and attempt.
- **Retention and permissions** — ensure investigators can access it, but unintended viewers cannot.
- **Downloaded copy** — verify it opens away from the runner and contains no machine-only links.
</WhereToCheck>

<WorkedExample title="the missing trace caused by success-only upload">
1. Browser tests write traces only for failures and exit 1 on checkout.
2. The following upload step has the provider's default success condition, so it is skipped.
3. The runner is destroyed; the log says only that a locator timed out.
4. The team changes the upload condition to always, keeps the test job red, and uploads the trace directory.
5. The next failure's trace reveals an overlay intercepting the click — evidence now survives without hiding failure.
</WorkedExample>

<Quiz question="Which statement correctly distinguishes an artifact from a cache?" options={[
  "Artifacts always make jobs faster; caches explain failures",
  "Artifacts preserve outputs or evidence from a run; caches reuse inputs to speed later runs",
  "They are identical names for permanent storage",
  "Caches are safe for secrets because users cannot download them"
]} answer={1} explain="Artifacts retain results and evidence for people or downstream jobs. Caches are disposable performance optimizations for reusable inputs and should not be the only copy of diagnostic evidence." />

<Flashcards cards={[
  { front: "Why must failure upload use an always condition?", back: "The test step is red precisely when its diagnostic artifacts are most valuable; success-only defaults skip them." },
  { front: "Artifact versus cache", back: "Artifact: run output/evidence. Cache: reusable input that speeds future work." },
  { front: "Useful artifact identity", back: "Suite, browser or target, commit SHA, job, and run attempt." },
  { front: "Why verify a downloaded report?", back: "A report can rely on paths or assets that existed only on the temporary runner." },
  { front: "Artifact security risk", back: "Logs, traces, screenshots, and videos can contain tokens, URLs, user data, or confidential UI." },
]} />

<Challenge>
Audit one pipeline's artifacts. For each, record purpose, generation path, failure behavior, typical
size, retention, access, and the decision it supports. Delete one useless upload and repair one missing
failure artifact.
</Challenge>

<AskCommunity prompt="Run [link/id] failed at [test]. Artifact upload status is [status], resolved path/file count is [values], and retention/access are [values]. What should I inspect next?">
Share metadata and directory shape, but redact tokens and personal data before sharing logs or screenshots.
</AskCommunity>

<Resources links={[
  { href: "https://docs.github.com/en/actions/using-workflows/storing-workflow-data-as-artifacts", title: "GitHub Docs — Store and share data with workflow artifacts", kind: "read" },
  { href: "https://playwright.dev/docs/ci-intro", title: "Playwright — Setting up CI", kind: "read" },
]} />

<Video href="https://www.youtube.com/watch?v=HUzCg0o0ScM" title="Playwright — Capture Screenshots and Record Videos After Test Execution — CommitQuality" minutes={9} />

<Takeaways points={[
  "Artifacts preserve run evidence after a disposable runner disappears.",
  "Upload diagnostics even when tests fail, without changing the job's red status.",
  "Keep reports self-contained and bind them to revision, job, target, and attempt.",
  "Set intentional size, retention, and access policies.",
  "Treat logs and visual evidence as potentially sensitive data; caches are not evidence archives.",
]} />

<Complete xp={10} />
