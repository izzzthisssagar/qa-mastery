---
title: "Run, exec, logs, and stop"
summary: "Control a container lifecycle without confusing image creation, process execution, log retrieval, graceful stopping, and removal."
tags: ["docker-and-containers-for-testers", "docker-hands-on", "track-e"]
updated: "2026-07-17"
related:
  - "docker-and-containers-for-testers/containers-in-plain-words/install-and-first-run"
  - "docker-and-containers-for-testers/docker-hands-on/ports-and-volumes"
  - "docker-and-containers-for-testers/docker-hands-on/debugging-a-container"
---

<Hook>`docker exec` does not start a stopped container, and `docker stop` does not delete one. Most beginner confusion disappears when you follow the main process and object state separately.</Hook>
<Callout type="analogy">A container is a theatre performance: `run` creates the stage and starts the lead actor, `exec` brings another actor onto an active stage, `logs` reads recorded dialogue, and `stop` asks the lead to finish.</Callout>
<Term define="The container lifecycle moves through created, running, paused, restarting, and exited states. Its main process determines whether it is running; removal is a separate action.">container lifecycle</Term>

## Commands with distinct jobs

- `docker run --name web -d IMAGE` creates and starts a new container.
- `docker exec web COMMAND` starts an additional process only while `web` runs.
- `docker logs --timestamps web` reads the configured logging stream.
- `docker stop --time 10 web` sends the stop signal, then kills after the timeout.
- `docker rm web` removes an exited container; `--rm` automates removal after exit.

<Callout type="tip">Name containers in exercises, inspect exit codes after stopping, and use `--rm` only when evidence is captured elsewhere.</Callout>
<Callout type="mistake">Using `exec` to patch a failing service. That change is unrepeatable and disappears with the container.</Callout>

<HotspotImage src="/notes/docker-and-containers-for-testers/docker-hands-on/run-exec-logs-and-stop.jpg" alt="A crane moving freight containers at a port" credit="Déplacement de containers au port — houaito affo daniel, CC BY-SA 4.0" creditHref="https://commons.wikimedia.org/wiki/File:D%C3%A9placement_de_containers_au_port.jpg" pins={[
  { x: 22, y: 72, label: "Create and run", desc: "A new container starts its configured main process." },
  { x: 56, y: 32, label: "Observe and enter", desc: "Logs and exec inspect a currently running workload." },
  { x: 84, y: 66, label: "Stop then remove", desc: "Process termination and object deletion are separate transitions." },
]}/>

<FlowAnimation title="A controlled diagnostic lifecycle" intervalMs={1300} nodes={[
  { label: "Run named container", desc: "Create and start with declared options." },
  { label: "Observe logs", desc: "Follow application output with timestamps." },
  { label: "Exec a read-only probe", desc: "Inspect process, files, or network without patching." },
  { label: "Stop gracefully", desc: "Allow the main process to handle its stop signal." },
  { label: "Inspect exit, then remove", desc: "Preserve evidence before cleanup." },
]}/>

<CodePlayground language="python" title="Run it — model lifecycle transitions (Python)" code={`state = "created"
for command, next_state in [("run", "running"), ("exec probe", "running"), ("stop", "exited")]:
    state = next_state
    print(f"{command} -> {state}")

# run -> running
# exec probe -> running
# stop -> exited`}/>

<CodePlayground language="java" title="Run it — model lifecycle transitions (Java)" code={`public class Main {
  public static void main(String[] args) {
    String[][] steps = {{"run", "running"}, {"exec probe", "running"}, {"stop", "exited"}};
    for (String[] step : steps) System.out.println(step[0] + " -> " + step[1]);
  }
}
/* run -> running
   exec probe -> running
   stop -> exited */`}/>

<FirstTime title="Your mission: operate one clean lifecycle">
<StepChecklist steps={[
  { text: "Run a named detached container", detail: "Choose a small service image and explicit version." },
  { text: "Read timestamped logs", detail: "Identify startup completion in application output." },
  { text: "Exec one read-only command", detail: "Confirm the process runs inside the existing container." },
  { text: "Stop, inspect exit, and remove", detail: "Keep state transitions distinct." },
]}/>
You can now explain what changed after every command.
</FirstTime>

<WhenItBreaks items={[
  { symptom: "`docker exec` says the container is not running.", fix: "Inspect state, exit code, error, and logs; start or recreate only after diagnosing the main process." },
  { symptom: "Stop ends in a forced kill.", fix: "Check signal handling, PID 1 behavior, and whether the timeout is realistic for cleanup." },
  { symptom: "Logs are empty.", fix: "Confirm the app writes to stdout/stderr and inspect its configured logging driver." },
]}/>

<WhereToCheck>
- `docker ps -a` for status.
- `docker inspect --format` for state, exit code, and error.
- `docker logs --timestamps --since ...` for application output.
- Daemon events when lifecycle timing is disputed.
</WhereToCheck>

<WorkedExample title="the container that stopped immediately">
1. QA runs a service detached and assumes it is healthy.
2. `docker exec` fails because the main process already exited.
3. `docker ps -a` shows exit code 1.
4. Logs reveal a missing required environment variable.
5. QA recreates with declared configuration instead of patching the stopped object.
</WorkedExample>

<Quiz question="What does `docker exec` do?" options={["Creates a new image", "Starts another process inside a running container", "Restarts any stopped container", "Deletes container logs"]} answer={1} explain="Exec requires a running container and launches an additional process in its namespaces." />
<Flashcards cards={[{ front: "run", back: "Create and start a new container." }, { front: "exec", back: "Start an additional process in a running container." }, { front: "stop", back: "Request graceful termination, then force after timeout." }]}/>
<Challenge>Capture a timeline containing create, start, log, exec, stop, exit, and remove events for one container.</Challenge>
<AskCommunity prompt="Container `[name]` moved from `[state]` to `[state]`; exit code is `[code]`; last logs are `[lines]`. Which process transition explains it?">Remove secrets before sharing logs.</AskCommunity>
<Resources links={[
  { href: "https://docs.docker.com/reference/cli/docker/container/run/", title: "Docker Docs — docker container run", kind: "read" },
  { href: "https://docs.docker.com/reference/cli/docker/container/exec/", title: "Docker Docs — docker container exec", kind: "read" },
  { href: "https://docs.docker.com/reference/cli/docker/container/logs/", title: "Docker Docs — docker container logs", kind: "read" },
]}/>
<Video href="https://www.youtube.com/watch?v=3c-iBn73dDE" title="Docker Tutorial for Beginners [FULL COURSE in 3 Hours] — TechWorld with Nana" minutes={166} />
<Takeaways points={["Run creates and starts; exec adds a process to a running container.", "Logs expose stdout and stderr through the logging driver.", "Stop and remove are separate operations.", "Inspect exit evidence before cleanup."]}/>
<Complete xp={10} />
