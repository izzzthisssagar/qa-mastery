---
title: "Install Docker and make the first run"
summary: "Verify the client-daemon boundary, run a pinned hello-world container, and capture enough evidence to distinguish installation failures from image and runtime failures."
tags: ["docker-and-containers-for-testers", "containers-in-plain-words", "track-e"]
updated: "2026-07-17"
related:
  - "docker-and-containers-for-testers/containers-in-plain-words/images-containers-and-registries"
  - "docker-and-containers-for-testers/docker-hands-on/run-exec-logs-and-stop"
  - "docker-and-containers-for-testers/docker-hands-on/ports-and-volumes"
---

<Hook>Seeing a Docker version proves the CLI binary runs. It does not prove the daemon is reachable, the image can be pulled, or a container process can start.</Hook>
<Callout type="analogy">The CLI is a remote control; the Docker daemon is the appliance. Reading the brand on the remote does not prove the appliance has power.</Callout>
<Term define="Docker uses a client-server architecture: the CLI sends API requests to a Docker daemon, which manages images, networks, volumes, and containers locally or remotely.">client-daemon boundary</Term>

## Prove each layer

1. Install Docker Desktop on supported desktop systems or Docker Engine through the supported Linux path.
2. Run `docker version`; require both Client and Server sections.
3. Run `docker info` and inspect server platform, architecture, and storage.
4. Pull and run a deliberate image reference such as `hello-world:latest` for learning.
5. Inspect the stopped container and remove it after collecting evidence.

<Callout type="tip">Follow the current platform-specific install page. Package names, virtualization requirements, and supported versions change.</Callout>
<Callout type="mistake">Adding users to the Linux `docker` group without recognizing that it grants root-level daemon control.</Callout>

<HotspotImage src="/notes/docker-and-containers-for-testers/containers-in-plain-words/install-and-first-run.jpg" alt="A mobile crane lifting a freight container" credit="Mobile container crane — Kcida10, CC0 1.0" creditHref="https://commons.wikimedia.org/wiki/File:Mobile_container_crane.jpeg" pins={[
  { x: 20, y: 72, label: "Client request", desc: "The CLI asks the daemon to create and start a container." },
  { x: 56, y: 29, label: "Daemon work", desc: "The engine pulls layers, configures isolation, and launches the process." },
  { x: 82, y: 61, label: "Visible result", desc: "Logs and exit status show whether the workload actually ran." },
]}/>

<FlowAnimation title="What hello-world proves" intervalMs={1300} nodes={[
  { label: "CLI reaches daemon", desc: "The API request crosses the configured Docker context." },
  { label: "Daemon resolves image", desc: "A local image is used or registry layers are pulled." },
  { label: "Container is created", desc: "Filesystem, network, and process configuration are prepared." },
  { label: "Process writes output and exits", desc: "Logs persist on the stopped container until removal." },
]}/>

<CodePlayground language="python" title="Run it — classify first-run evidence (Python)" code={`checks = {"client": True, "server": True, "process_exit": 0}
for name, value in checks.items():
    status = "PASS" if value is True or value == 0 else "FAIL"
    print(f"{name}: {status}")

# client: PASS
# server: PASS
# process_exit: PASS`}/>

<CodePlayground language="java" title="Run it — classify first-run evidence (Java)" code={`import java.util.*;
public class Main {
  public static void main(String[] args) {
    Map<String,Integer> checks = new LinkedHashMap<>();
    checks.put("client", 1); checks.put("server", 1); checks.put("process_exit", 0);
    checks.forEach((name, value) -> {
      boolean pass = name.equals("process_exit") ? value == 0 : value == 1;
      System.out.println(name + ": " + (pass ? "PASS" : "FAIL"));
    });
  }
}
/* client: PASS
   server: PASS
   process_exit: PASS */`}/>

<FirstTime title="Your mission: make a traceable first run">
<StepChecklist steps={[
  { text: "Use the official install path", detail: "Select the page for your exact OS and architecture." },
  { text: "Verify client and server", detail: "Capture `docker version` without secrets." },
  { text: "Run a named hello-world container", detail: "A name makes later inspection unambiguous." },
  { text: "Inspect exit and logs, then remove", detail: "Prove the lifecycle rather than stopping at terminal output." },
]}/>
Your installation evidence now reaches the workload process.
</FirstTime>

<WhenItBreaks items={[
  { symptom: "The client prints a version but cannot connect.", fix: "Start Docker Desktop or the daemon, then inspect the active Docker context and socket permission." },
  { symptom: "Pull times out or returns unauthorized.", fix: "Check proxy, DNS, registry reachability, rate limits, and credential configuration." },
  { symptom: "Image platform does not match the host.", fix: "Choose a multi-platform image or an explicit supported platform and record emulation use." },
]}/>

<WhereToCheck>
- `docker context show` and `docker context inspect`.
- Client and Server sections of `docker version`.
- Docker Desktop diagnostics or Linux daemon logs.
- `docker inspect` for exit code, error, platform, and image.
</WhereToCheck>

<WorkedExample title="the misleading version check">
1. A new laptop prints a Docker CLI version.
2. Every `docker run` returns “cannot connect to the Docker daemon.”
3. `docker context show` reveals a stale remote context.
4. QA selects the intended local context and verifies the Server section.
5. The named hello-world container exits zero and its logs are captured.
</WorkedExample>

<Quiz question="What does `docker version` prove only when it shows both Client and Server sections?" options={["Every image is safe", "The CLI can communicate with a Docker daemon", "All containers are running", "The host matches production"]} answer={1} explain="The Server section requires a successful client-daemon exchange; it does not prove broader parity or image safety." />
<Flashcards cards={[
  { front: "Docker client", back: "CLI or API consumer that sends requests." },
  { front: "Docker daemon", back: "Server that manages container objects and processes." },
  { front: "Docker context", back: "Named endpoint and connection configuration used by the client." },
]}/>
<Challenge>Document a four-check installation smoke test that distinguishes CLI, daemon, registry, and container-process failures.</Challenge>
<AskCommunity prompt="`docker version` shows `[sections]`; context is `[name]`; first-run error is `[exact text]`. Which boundary failed?">Remove tokens, usernames, and private registry details.</AskCommunity>
<Resources links={[
  { href: "https://docs.docker.com/engine/install/", title: "Docker Docs — Install Docker Engine", kind: "read" },
  { href: "https://docs.docker.com/desktop/", title: "Docker Docs — Docker Desktop", kind: "read" },
]}/>
<Video href="https://www.youtube.com/watch?v=3c-iBn73dDE" title="Docker Tutorial for Beginners [FULL COURSE in 3 Hours] — TechWorld with Nana" minutes={166} />
<Takeaways points={["A CLI version alone does not prove daemon connectivity.", "Install from the current official path for the exact platform.", "The first run should verify registry, runtime, logs, and exit status.", "Docker daemon access is security-sensitive."]}/>
<Complete xp={10} />
