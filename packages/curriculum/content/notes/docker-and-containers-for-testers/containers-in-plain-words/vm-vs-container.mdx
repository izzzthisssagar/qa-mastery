---
title: "Virtual machine vs container"
summary: "Separate hardware virtualization from process isolation, then choose the boundary that makes a QA environment repeatable without pretending containers are tiny virtual machines."
tags: ["docker-and-containers-for-testers", "containers-in-plain-words", "track-e"]
updated: "2026-07-17"
related:
  - "docker-and-containers-for-testers/containers-in-plain-words/images-containers-and-registries"
  - "docker-and-containers-for-testers/containers-in-plain-words/why-qa-cares"
  - "docker-and-containers-for-testers/containers-in-plain-words/install-and-first-run"
---

<Hook>A container is not a lightweight computer. It is an isolated process whose strongest dependency—the host kernel—is easy to forget until a test behaves differently on another operating system.</Hook>

<Callout type="analogy">A VM rents a furnished house with its own utilities; a container rents a locked workshop in a shared building. Both provide boundaries, but they share different layers.</Callout>

<Term define="A virtual machine emulates or virtualizes hardware and boots a guest operating system. A container isolates processes and filesystems while sharing the host kernel through namespaces, cgroups, and related controls.">virtualization boundary</Term>

## Compare the actual boundaries

| Question | Virtual machine | Container |
|---|---|---|
| Kernel | Guest kernel | Host kernel |
| Startup | Boots an OS | Starts a process |
| Image contents | OS plus application | Application and user-space dependencies |
| Isolation | Hardware-level boundary | Process-level boundary |
| QA fit | Cross-OS and kernel testing | Fast, repeatable service environments |

<Callout type="tip">Use a VM when the kernel or operating system is part of the test target. Use a container when the goal is packaging and repeating a user-space service.</Callout>

<Callout type="mistake">Calling containers “mini VMs” hides kernel sharing and leads to false portability claims.</Callout>

<HotspotImage src="/notes/docker-and-containers-for-testers/containers-in-plain-words/vm-vs-container.jpg" alt="Stacks of standardized freight containers at a terminal" credit="Container port — Andrew McMillan, public domain" creditHref="https://commons.wikimedia.org/wiki/File:Container_port_(1).jpg" pins={[
  { x: 23, y: 31, label: "Shared terminal", desc: "Containers share host infrastructure just as freight boxes share terminal equipment." },
  { x: 58, y: 53, label: "Isolated contents", desc: "Each box carries its own payload and user-space dependencies." },
  { x: 83, y: 72, label: "Standard interface", desc: "A consistent package boundary makes handling repeatable across compatible hosts." },
]}/>

<FlowAnimation title="From host to test process" intervalMs={1400} nodes={[
  { label: "Host kernel", desc: "Owns scheduling, networking, and device access." },
  { label: "Runtime requests isolation", desc: "Namespaces and resource controls define the boundary." },
  { label: "Image filesystem is mounted", desc: "Read-only layers supply user-space files." },
  { label: "Application process starts", desc: "The container lives while its main process lives." },
]}/>

<CodePlayground language="python" title="Run it — compare packaged layers (Python)" code={`vm = ["guest kernel", "system services", "test app"]
container = ["user-space libraries", "test app"]
print(f"VM packages {len(vm)} layers: {', '.join(vm)}")
print(f"Container packages {len(container)} layers and shares the host kernel")

# VM packages 3 layers: guest kernel, system services, test app
# Container packages 2 layers and shares the host kernel`}/>

<CodePlayground language="java" title="Run it — compare packaged layers (Java)" code={`import java.util.*;
public class Main {
  public static void main(String[] args) {
    List<String> vm = List.of("guest kernel", "system services", "test app");
    List<String> container = List.of("user-space libraries", "test app");
    System.out.println("VM packages " + vm.size() + " layers: " + String.join(", ", vm));
    System.out.println("Container packages " + container.size() + " layers and shares the host kernel");
  }
}
/* VM packages 3 layers: guest kernel, system services, test app
   Container packages 2 layers and shares the host kernel */`}/>

<FirstTime title="Your mission: choose the correct boundary">
<StepChecklist steps={[
  { text: "Name what the test must vary", detail: "Application dependencies, OS user space, or the kernel itself?" },
  { text: "Record the host constraint", detail: "Linux containers require a compatible Linux kernel, directly or through a VM." },
  { text: "Choose container or VM", detail: "Make the boundary an explicit test-design decision." },
  { text: "Write one invalid claim", detail: "Document what the chosen boundary cannot prove." },
]}/>
You now have an isolation decision, not a fashionable default.
</FirstTime>

<WhenItBreaks items={[
  { symptom: "A Linux container behaves differently on macOS or Windows.", fix: "Inspect Docker Desktop's Linux VM boundary and architecture; do not assume the host kernel is identical." },
  { symptom: "A kernel-specific defect cannot be reproduced.", fix: "Test on the target kernel or a representative VM rather than relying on user-space packaging." },
  { symptom: "The container consumes too many resources.", fix: "Set and observe CPU and memory limits; isolation does not imply unlimited capacity." },
]}/>

<WhereToCheck>
- `docker inspect` for runtime configuration and platform.
- `docker info` for server OS, architecture, and runtime.
- `/proc/1/cgroup` inside a Linux container for process context.
- The target production kernel and architecture requirements.
</WhereToCheck>

<WorkedExample title="the test that needed a VM">
1. A filesystem watcher passes in a Linux container on a macOS laptop.
2. Production uses a specific Linux kernel and storage driver.
3. The container reproduces user-space packages but not the production kernel path.
4. QA reruns it in a representative VM and reproduces the missed event.
5. The test plan now distinguishes packaging coverage from kernel coverage.
</WorkedExample>

<Quiz question="Which statement identifies the central VM/container difference?" options={["Containers have no operating-system files", "A VM boots a guest kernel while a container shares the host kernel", "VMs cannot run services", "Containers always provide stronger isolation"]} answer={1} explain="The kernel boundary is the key distinction; performance and security depend on configuration and workload." />

<Flashcards cards={[
  { front: "VM boundary", back: "Virtualized hardware plus a guest operating system and kernel." },
  { front: "Container boundary", back: "Isolated processes and user space sharing a host kernel." },
  { front: "QA decision", back: "Choose based on which layers the test must control or vary." },
]}/>

<Challenge>Take one current environment test and state precisely which defects a container can reproduce and which require a representative VM or host.</Challenge>

<AskCommunity prompt="Our test needs to vary `[layer]` on `[target platform]`. We chose `[container/VM]` because `[boundary]`. What host dependency are we still missing?">Include host OS, architecture, and the behavior under test.</AskCommunity>

<Resources links={[
  { href: "https://docs.docker.com/get-started/docker-concepts/the-basics/what-is-a-container/", title: "Docker Docs — What is a container?", kind: "read" },
  { href: "https://docs.docker.com/engine/security/", title: "Docker Docs — Engine security", kind: "read" },
]}/>

<Video href="https://www.youtube.com/watch?v=Gjnup-PuquQ" title="Docker in 100 Seconds — Fireship" minutes={2} />

<Takeaways points={[
  "A VM boots a guest kernel; a container shares the host kernel.",
  "Containers package user-space dependencies around an isolated process.",
  "Kernel and cross-OS validation may require a representative VM or host.",
  "Isolation, portability, and security are properties to verify, not slogans.",
]}/>

<Complete xp={10} />
